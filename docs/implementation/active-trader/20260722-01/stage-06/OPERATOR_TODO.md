# Operator TODO — after Stage 6
- None blocking. /v3-next is a separate read-only fixtures bundle; /v3 untouched.
- Carried: Stage 5 Moomoo credential lockout (resolve login via moomoo recovery/support, then
  complete the OpenAPI regulatory agreement) — required before any live data; unrelated to Stage 6.
- When /v3-next later reads the live Stage 4 API, decide the localhost dev CORS origin (Stage 4 TODO).
