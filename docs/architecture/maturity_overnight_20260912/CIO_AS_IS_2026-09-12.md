# AS-IS — what this system actually does, 2026-09-12

Campaign `trade-ai-maturity-overnight-20260912`. Every claim carries the status
it earned: OBSERVED_CURRENT (seen on the served epoch during this window),
OBSERVED_PRIOR_EPOCH, INTEGRATED (built and tested, not yet deployed),
HERMETIC_ONLY, PARTIAL, ABSENT, BLOCKED.

## The spine, as it ran

An hourly cron wake selects unconsumed research, grounds it in subject-scoped
memory, decides deterministically, and writes durable wake / receipt /
commitment rows to an external state root that survives promotes.

Observed on the served epoch (`eb648174a`, opened 2026-09-11T22:06Z):

```
2026-09-12T00:00Z   3 wakes   subj 0bc81168 grounded 1 fact, 879 wrong-subject rows excluded
2026-09-12T01:00Z   3 wakes   subj 0bc81168 grounded 1 fact, 879 wrong-subject rows excluded
                    4 receipts, all effect_kind=changed_question
                    6 governed commitments, all frozen_at < due_at
```

`stale_memory_retained_with_decay` on both grounded wakes: the fact is far past
the old 168h cliff and is carried with a decay weight rather than discarded.
That behaviour is the L2 repair from the previous campaign, still holding.

**Status: OBSERVED_CURRENT.**

## What produces the evidence, and whether anything watches it

Fourteen scheduled lanes were undeclared until this campaign, including every
lane the maturity ladder depends on. Declared and read back against live data:

| lane | signal | verdict |
|---|---|---|
| `persistent-wake-hourly` | `wakes.jsonl` mtime | LIVE 0.10h |
| `governed-research-producer` | health file `as_of` | LIVE 0.36h |
| `wake-selection-feed` | feed mtime | LIVE 0.20h |
| `identity-sweep-stage0` | `hermes_external_research.identity_tagged_at` | LIVE 0.11h |
| `material-change-detector-stage1` | `material_changes.created_at` | SLOW 0.61h |
| `material-change-notifier-stage2` | `material_changes.notified_at` | SLOW 0.50h |
| `due-diligence-questions` | `due_diligence_questions.created_at` | SLOW 0.43h |
| `document-mentions-backfill` | `document_mentions.observed_at` | LIVE 0.66h |
| six deletion-only / timestamp-less lanes | none | UNVERIFIABLE, declared as such |

**Status: OBSERVED_CURRENT.**

## Judgment

DeepSeek Flash authored five grounded judgments on 2026-09-11 between 15:00Z and
19:00Z — real calls, `model_returned` matching `model_requested`, latency
2306–3786ms, five distinct output digests. **On prior epochs.** Zero on the
served epoch.

Since then the lane has refused, correctly and for two different reasons:
`budget_cap` while the shared daily pool was exhausted, then `offpeak_deferred`
once the operator bulk window closed at 01:00Z. `provider_call_attempts=0` on
the off-peak refusals with `live_provider_allowed=true` — it declines while
permitted to spend, which is the behaviour the rail exists to produce.

What those five records did **not** contain: a cost (every paid call recorded
`$0.00`), a prompt digest, a release, or an id that distinguished one judgment
from another — all five shared `jdg_97ab0292bcd3339e76c474c2` while two
persisted views an hour apart carried that id at confidence 0.72 and 0.60.

**Status: OBSERVED_PRIOR_EPOCH for authoring; INTEGRATED for the record repairs.**

## Research circulation

The FREE_FIRST_ONLY Hermes circulation lane has **not completed a single run
since 2026-09-08**. Every run is SIGTERM'd at the unit's `TimeoutStartSec=900`
— 93 consecutive — because wall time grew with the universe (2m42s on 08-23,
14m40s on 09-07) and the work was unbounded. The receipt is written only after
the work returns, so each kill destroyed the run and its record together.

The dedicated health predicate reported `healthy: true` throughout, while
printing the 4.4-day-old timestamp and the non-served SHA in its own output.

**Status: ABSENT (lane); INTEGRATED (bound, cursor, failure receipt, and a
health predicate that can go false).**

## Outcome learning

The contract exists — `GovernedCommitmentOutcome@v1`, the outcome vocabulary,
a self-evaluation prohibition, an append-only ledger that preserves refuted
outcomes, and a calibration store. Nothing revisits a commitment: `0` cron
entries and `0` systemd units, and `evaluate_outcome`'s single caller ran it on
the same line that minted the prediction.

224 durable records: 99 predictions, 125 wake observations. 0 past due (earliest
2026-09-17). **0 of the 99 predictions are falsifiable.**

**Status: ABSENT.**

## Communication

On the served epoch: 11 `communication_events`, 1 genuinely SETTLED with a
provider ack, 9 UNSETTLED, 1 UNKNOWN_LEGACY, **0 inbound**. Four consumption
receipts. One poller process for the bot token — the one-poller-per-token risk
previously deferred is not currently realised.

**Status: PARTIAL.**

## Where the evidence physically lives, and who reads it

Producers running from the canonical tree write `…/trade-ai-v12-rebuild/data/…`.
The API reads `CURRENT/data/… -> …/persistent-state/data/…`. Two inodes. 164
served files are behind their producers; 19 files in `data/runtime` flow the
other way. `data/health` is the only clean tree.

This is why the Oscillator board serves a 17-day-old reading, and why MVL
counters read low from the served plane (`sentinel_reviews.jsonl`: 140 producer,
104 served).

**Status: ABSENT (one store); the detector is INTEGRATED.**

## Authority and safety, as observed

- `MBI_BEHAVIOR=0` throughout; every artifact this campaign touched carries
  `mbi_behavior: 0` and `authority: READ_ONLY_ADVISORY`.
- No broker, order, execution, credential, account or financial-guardrail path
  was modified. Darwin scorecards record `model_calls: 0`, `cost_usd: 0.0`.
- Nightly reflection records `mutates_production: false` and
  `auto_promotions: 0` across all 14 of its records.
- The L3 lane refuses on ungrounded context before the network, and refuses a
  same-provider critic before any call.
