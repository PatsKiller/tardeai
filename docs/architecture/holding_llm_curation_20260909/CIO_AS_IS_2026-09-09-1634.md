Status:      ACTIVE
as_of:       2026-09-09-1634 America/New_York
utc:         2026-09-09T20:34:00Z
Topic:       Holding-drawer LLM curation + CIO Flash consensus
Branch:      feat/holding-llm-curation-cio-flash
MBI_BEHAVIOR: 0
Supersedes:  drawer UX that treated Hermes external theses as primary CIO narrative

# CIO / Command Center — AS-IS (holding LLM curation) — 2026-09-09-1634

**Version stamp (America/New_York):** `2026-09-09-1634`

```
LEGEND (status icons — use consistently across AS-IS / FUTURE / GAP)
  █  OBSERVED_LIVE     scheduled or live path; durable output verified on CURRENT epoch
  ◈  INTEGRATED        wired end-to-end on serving SHA (producer→store→consumer)
  ◇  DOCKED            code + schema on CURRENT; consumer or organic proof incomplete
  ▓  PARTIAL           runs, but incomplete / degraded / consumer unproven
  ░  UNWIRED           code exists and is correct; nothing calls it or consumes it
  ◌  HERMETIC_ONLY     tests/helpers PASS; must NOT be quoted as OBSERVED
  ▒  DOCUMENTATION_ONLY docs/helpers without producer+consumer+organic evidence bar
  ✗  ABSENT / DARK     never executed, no producer, or produces nothing in recorded history
  ⛔ BLOCKED           structural/policy blocker (grant scope, secret, stuck checkpoint, etc.)
```

## Pin context

| Field | Value |
|---|---|
| Prior served pin (pre this PR) | `e651b2d77` maturity-gap promote |
| This change | branch `feat/holding-llm-curation-cio-flash` (not yet merged/deployed at doc stamp) |
| Tests | ◌ `tests/test_holding_llm_curation.py` 6 passed |

## Architecture — as-built BEFORE this change (diagnosis)

```
Click holding (SPCX)
        │
        ▼
 DetailDrawer ──GET /api/v2/hermes/intel/{SYM}──┐
        │                                       │
        ├─ CIO synthesis evidence ◇             │
        │   watchlist_final_synthesis           │
        │   dual = Grok+ChatGPT only            │
        │   Flash = fallback-only / rare        │
        │   refusal prose could be upserted     │
        │                                       │
        └─ External LLM intelligence ▓          │
            hermes_external_research            │
            cron: grok,chatgpt every 2h         │
            deepseek: not scheduled             │
            no freshness max-age in drawer      │
            LLM_META: no DeepSeek brand         │
```

```mermaid
flowchart TD
  click[Holding click]
  drawer[DetailDrawer]
  api[hermes_intel API]
  ext[(hermes_external_research)]
  syn[(watchlist_final_synthesis)]
  cron[Cron grok chatgpt]
  dual[CIO dual Grok ChatGPT]
  click --> drawer --> api
  api --> ext
  api --> syn
  cron --> ext
  dual --> syn
```

## Workflow — what the operator saw (SPCX example)

1. Open holding → drawer loads intel.
2. CIO panel showed `[DUAL-CONSENSUS]` IGNORE after Grok=HOLD vs ChatGPT=IGNORE, plus refusal error text.
3. External cards: ChatGPT + Grok fresh (9/7), Deepseek stale (8/22) or absent if skip/DQ.
4. Home briefing footer still claimed “Ollama first” while enrichment is cloud OAuth + fail-closed cache.

## Domain status (AS-IS at diagnosis)

| Domain | Status | Notes |
|---|---|---|
| Hermes free OAuth external cron | ◈ | grok,chatgpt every 2h |
| Hermes Deepseek external | ░ / ⛔ | paid window; not on cron; skips invisible |
| CIO dual Grok+ChatGPT | ◈ | writes recommendation |
| CIO Flash as voter | ✗ | fallback only |
| Drawer freshness badges | ✗ | none |
| Refusal fail-closed | ▓ | normalized string still upsertable historically |
| Canonical CIO shared spine | ▓ | broker exists; drawer not labeled SoR |
| Home Ollama copy | ▓ | stale UX string |

## What this implementation changes (code on branch — ◌ until deploy)

- CIO default lanes: **Grok + ChatGPT + DeepSeek Flash 4.1** (`deepseek-v4-flash`) as voting participants.
- Flash skip statuses recorded (`SKIPPED_PEAK` / `CAP` / `BREAKER` / …) — no invented vote.
- Refusal/error narratives **do not upsert** (keep prior good synthesis).
- API: freshness classes, lane_status_summary, fuller CIO card fields.
- Drawer: canonical CIO card + challenger labeling + DeepSeek brand + on-demand Flash challenger button.
- Cron comment: Deepseek remains **not** on blanket top-20; on-demand via `POST /api/v2/hermes/curate-symbol`.


## Detailed component map (as-built producers / stores / consumers)

| Component | Path | Role | Status at diagnosis |
|---|---|---|---|
| DetailDrawer | `apps/command-center-v3/src/components/DetailDrawer.tsx` | Holding click UI | ▓ dual panels, weak Deepseek UX |
| Hermes intel API | `scripts/api_v2.py::_hermes_intel` | Aggregates external + CIO | ▓ no freshness |
| External researcher | `scripts/hermes_external_researcher.py` | Lane callers incl deepseek | ◈ code; ░ cron for DS |
| Top-20 curate | `scripts/hermes_top20_external_intel.py` | Cron fan-out | ◈ grok,chatgpt only |
| Subject enhance | `scripts/hermes_subject_enhance.py` | Position/proposal enhance | ◈ grok,chatgpt |
| CIO synthesis | `scripts/process_watchlist_agent_jobs.py` | Final recommendation writer | ◈ dual; ✗ Flash voter |
| Dual re-run | `scripts/rerun_cio_dual_consensus.py` | Overnight cross-check | ◈ OAuth dual |
| Final synthesis broker | `scripts/lib/data_broker/final_synthesis.py` | Shared read model | ◈ exists; ▓ incomplete Flash fields |
| Home briefing | `llm_intelligence_enrichment.py` + HomeTrustRender | Separate product | ◈ cloud; ▓ wrong footer |
| Lane registry | `config/lane_registry.json` | Cron truth | ◈ documents grok,chatgpt |

## Workflow — free OAuth dual (pre-change)

```
Maria / Steph / Risk results
        │
        ▼
run_synthesis(prompt)
        │
        ├─ llm_lane.generate(grok)
        ├─ llm_lane.generate(chatgpt)  [cap CIO_DUAL_CHATGPT_CAP]
        │
        ▼
agree? ──yes──► consensus = shared rec
   │
   no ──► cautious = min(_CONSERV)
        │
        ▼
parse JSON narrative
        │
        ├─ refusal? ── historically normalize to "LLM error:…" THEN STILL UPSERT (bad)
        └─ post-LLM income/RSI gates → HOLD overrides
        │
        ▼
UPSERT watchlist_final_synthesis
        │
        ▼
Drawer shows recommendation + 320-char snip
```

## Workflow — Hermes external card (pre-change)

```
cron every 2h: hermes_top20 --lanes grok,chatgpt
        │
        ▼
for symbol in top-20 ∪ held:
  skip if success within FRESH_HOURS=12
  else call_external(lane) → hermes_external_research
        │
        ▼
Drawer DISTINCT ON (lane) success statuses only
  → Deepseek skips never surface
  → month-old success rows still render
```

## Operator-visible failure modes (catalogued)

1. **Refusal as IGNORE prose** — ChatGPT refuses synthesis; dual still stores cautious IGNORE with error prefix.
2. **Silent Deepseek** — peak/cap/DQ → status not in (sent,ok,…) → empty panel.
3. **Stale challenger** — last success 8/22 next to 9/7 OAuth looks like “Deepseek broken”.
4. **Home month-old briefing** — quality gate fail-closed keeps last good cache; footer blamed Ollama incorrectly.
5. **Two SoRs** — Hermes thesis vs CIO recommendation disagree without hierarchy.

## Implementation delta on this branch (code truth)

| Change | Files | Hermetic |
|---|---|---|
| `holding_intel_freshness.py` | new lib | ◌ tested |
| `_synthesis_lanes` triple + Flash status | process_watchlist_agent_jobs.py | ◌ unit reconcile |
| Refusal no-upsert | process_watchlist_agent_jobs.py | ◌ |
| `_hermes_intel` freshness + lane_status + CIO fields | api_v2.py | ◌ |
| `POST /hermes/curate-symbol` | api_v2.py | ◌ |
| DetailDrawer SoR + challengers + Flash brand | DetailDrawer.tsx | ◌ |
| HomeTrustRender footer | HomeTrustRender.tsx | ◌ |
| Broker Flash fields | final_synthesis.py | ◌ |
| lane_registry comment | lane_registry.json | ◌ |

## Safety rails preserved

- MBI_BEHAVIOR=0
- No broker / order / credential mutation in this wave
- LEGACY_CIO_REVIEW classification unchanged (not AUTHORITATIVE_CIO_ACTION)
- Deepseek Pro not introduced for CIO
