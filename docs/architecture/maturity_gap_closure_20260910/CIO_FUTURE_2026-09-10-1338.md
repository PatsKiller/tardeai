Status:      ACTIVE
as_of:       2026-09-10-1338 America/New_York (2026-09-10 13:38:54 EDT(-0400))
utc:         2026-09-10T17:38:54Z
Measured at: target spec, not runtime — full-maturity bar restated, not diluted
Canonical repo path: docs/architecture/maturity_gap_closure_20260910/CIO_FUTURE_2026-09-10-1338.md
Authority:   full-maturity target (unchanged bar) + dated reading
Campaign:    /home/johnclaw/trade-ai-campaigns/maturity-gap-closure-20260909/
MBI_BEHAVIOR: 0
Supersedes:  CIO_*_2026-09-10-0215.md
See also:    paired AS-IS / FUTURE / GAP / HONEST_MATURITY at stamp 2026-09-10-1338

# CIO Agent — FUTURE (full maturity target) — 2026-09-10-1338

**Version stamp (America/New_York):** `2026-09-10-1338`
**UTC:** `2026-09-10T17:38:54Z`

The bar is unchanged from prior stamps. Restated, not diluted. What changed this
wave is that production finally reaches it on volume — and the one clause that
still fails is provenance, not production.

```
LEGEND (FUTURE status glyphs — plus the AS-IS set)
  ◆ new                    capability that did not exist at the prior stamp
  ★ proven-unattended      scheduled + unattended, durable output OBSERVED
  █ OBSERVED_LIVE ◈ INTEGRATED ◇ DOCKED ▓ PARTIAL ░ UNWIRED
  ◌ HERMETIC_ONLY ▒ DOCUMENTATION_ONLY ✗ ABSENT/DARK ⛔ BLOCKED
```

## The four additions (unchanged bar)

1. **Judgment** — `AgentView@v1` with critic and falsifier, citations mandatory,
   `MBI_BEHAVIOR=0`, **and `source_sha` stamped to the serving SHA**.
2. **Commitments** — versioned commitments with `confirmed` / `refuted` /
   `expired` / `insufficient_evidence`.
3. **Scoring** — calibration from settled outcomes; `REFUTED` never hidden;
   minimum-sample rules.
4. **Self-repair** — human-governed detect → propose → PR → approve → deploy →
   verify; non-financial first.

## Target state, per domain

| Domain | Full-maturity definition |
|---|---|
| Identity | Every long-lived daemon self-exits on CURRENT mismatch; recycle is part of promote; the executing tree is asserted by a gate; **every evidence record carries its producing SHA** |
| Wake continuity | One durable, release-independent state root; promote never forks evidence; three contiguous organic cycles routinely available |
| Comms | Gateway-owned `SETTLED` plus inbound consumption on the serving SHA, organic, post-epoch |
| Research | Canonical `brave_router` + stable source index + free-first + cost policy + a producer that recurs, emitting organic receipts **stamped with its SHA** |
| Librarian / retention | Scheduled dry-run → bounded apply with holds and tombstones; LLM curation budget-gated; never broker authority |
| Judgment | `AgentView@v1` produced on the serving SHA, with critic and citations |
| Commitments | Versioned, durable, outcome-evaluated |
| Scoring | Calibration from settled outcomes only |
| Self-repair | Governed apply path, non-financial surfaces first |
| Docs / Drive | AS-IS / FUTURE / GAP / HONEST trio current, **in git**, and Kersta/Drive 13/13 hash-verified |

## Acceptance language (never dilute)

- Organic means `provenance.trigger = schedule_slot`. Manual, replay, backfill,
  fixture, synthetic and controlled-canary triggers are excluded, permanently.
- `effect_kind='none'` is legal and is **never** consumption evidence.
- Hermetic tests establish correctness, never observation.
- Exit code 0 is not evidence of work; the durable artifact is.
- Three **contiguous** organic cycles. (Now satisfied on volume — 36 cycles — but
  the *provenance* clause on the SHA is the remaining gate.)

## The full-maturity workflow (end-to-end)

```
REAL TRADE AI EVENT → Security/Sector/Catalyst → OPERATOR → S0
→ CANONICAL ENTITY/IDENTITY (NarrativeSubjectLink@v1)        ◆ new, on main not served
→ MATERIALITY → INSTRUMENT RECORD → RESEARCH GAP
→ free-first research (governed_brave_router, budget_governed)   █ live, llm=null
→ [JUDGMENT] AgentView@v1 + critic + citations               █ organic, ◆ source_sha pending
→ SPECIALIST DISPATCHER → CIOCouncilSynthesis
→ [COMMITMENT] CommitmentRecord@v2 (confirmed/refuted/expired)   ▓ OPEN+FROZEN only; refuted path unproven
→ CIOOperatorProduct → NOTIFICATION → DELIVERY RECEIPT/DEDUPE
→ OutcomeCheckpoint → OUTCOME → [SCORING]                       ◌ hermetic; no settled-outcome calibration
→ REVIEW_READY → MBI_BEHAVIOR
```

Every node above carries the *same* unresolved clause: **none of the durable
records stamps the SHA that produced it**. Full maturity means the evidence names
its own source.

## Build order from here (re-prioritised)

1. **Stamp `source_sha` into every producer** (wake, receipt, commitment,
   AgentView, research object, producer health). This is now the top blocker —
   it is the only clause separating "produced organically" from "proven on the
   serving SHA".
2. Promote `origin/main` (`1afe3c3d8`) to CURRENT so the served release closes
   the 7-commit identity gap and picks up narrative composition.
3. Wire `refuted` / `expired` outcomes so the 6 `FROZEN` commitments resolve into
   a falsifiable terminal state, not just a freeze.
4. Stand up scoring calibration once AgentViews carry their SHA and settled
   outcomes exist to calibrate from.
5. Publish this doc set to git and Drive (13/13).
