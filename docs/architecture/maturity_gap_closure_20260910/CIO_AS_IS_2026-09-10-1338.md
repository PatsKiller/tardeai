Status:      ACTIVE
as_of:       2026-09-10-1338 America/New_York (2026-09-10 13:38:54 EDT(-0400))
utc:         2026-09-10T17:38:54Z
Measured at: live host truth on served release 9c3f4871c-main-exact-phase2-20260910-112135
Canonical repo path: docs/architecture/maturity_gap_closure_20260910/CIO_AS_IS_2026-09-10-1338.md
Authority:   dated reading — not a behaviour spec; hermetic ≠ OBSERVED
Campaign:    /home/johnclaw/trade-ai-campaigns/maturity-gap-closure-20260909/
MBI_BEHAVIOR: 0
Supersedes:  CIO_*_2026-09-10-0215.md (emailed 2026-09-10 ~13:37 ET)
See also:    paired AS-IS / FUTURE / GAP / HONEST_MATURITY at stamp 2026-09-10-1338

# CIO Agent — AS-IS (as-built) — 2026-09-10-1338

**Version stamp (America/New_York):** `2026-09-10-1338` (`2026-09-10 13:38:54 EDT(-0400)`)
**UTC:** `2026-09-10T17:38:54Z`

```
LEGEND (status icons — use consistently across AS-IS / FUTURE / GAP)
  █  OBSERVED_LIVE     scheduled or live path; durable output verified on CURRENT epoch
  ◈  INTEGRATED        wired end-to-end on serving SHA (producer→store→consumer)
  ◇  DOCKED            code + schema on CURRENT; consumer or organic proof incomplete
  ▓  PARTIAL           runs, but incomplete / degraded / consumer unproven
  ░  UNWIRED           code exists and is correct; nothing calls it or consumes it
  ◌  HERMETIC_ONLY     tests/helpers PASS; must NOT be quoted as OBSERVED
  ▒  DOCUMENTATION_ONLY docs/helpers without producer+consumer+organic evidence bar
  ✗  ABSENT / DARK     never executed, no producer, or produces nothing in recorded history
  ⛔ BLOCKED           structural/policy blocker (grant scope, secret, stuck checkpoint, etc.)
  M5_CANDIDATE         scheduled + unattended proven; durability OBSERVED clause still open
```

## Pin / SHA context (authoritative)

The organic evidence chain is now **live and producing**, but the git identity
chain does **not** close this wave: the served release is 7 commits behind
`origin/main`.

| Surface | Value |
|---|---|
| `origin/main` | `1afe3c3d819b9cb08fdc5d8eea05c8f60d4920e5` |
| Served release (`CURRENT`) | `9c3f4871c-main-exact-phase2-20260910-112135` |
| `SOURCE_COMMIT` / `BUILD_SHA` | `9c3f4871c7222229d2d5c073d0448e83e5d0261d` |
| Executing dev tree | `1afe3c3d819b9cb08fdc5d8eea05c8f60d4920e5` (detached, == main) |
| Chain closes? | **NO** — served `9c3f4871c` is 7 commits behind main `1afe3c3d8` |

The 7 commits between served and main are the CIO **narrative-subject-identity**
and **narrative-composition** merges (PR #953 `NarrativeSubjectLink@v1`, PR #954
novelty gate + governed model narrator), plus the feature-to-live runbook
(PR #952). They are on `origin/main` and in the dev tree but **not on the served
release**.

## The one finding that dominates this stamp

Every durable evidence record writes `source_sha = "unknown"`.

| Evidence surface | Rows | `source_sha` |
|---|---|---|
| `wakes.jsonl` | 36 | `unknown` (all) |
| `receipts.jsonl` | 36 | `unknown` (all) |
| `commitments.jsonl` | 42 | `unknown` (all) |
| `agent_views.jsonl` | 9 | `unknown` (all) |
| `research_objects.jsonl` | 15 | `unknown` (all) |
| `research_producer_health.json` | 1 | `unknown` |

The producers are running and their output is durable, but **nothing stamps the
serving SHA into the evidence**. The FUTURE bar requires every `AgentView@v1` and
every consumption receipt to be *produced on the serving SHA*. As-built, the
provenance field is a placeholder, so no downstream consumer can prove which code
produced a given wake, receipt, or judgment. This is the new ceiling.

## Persistent wake — production is organic and beyond the M2 bar

The release-independent root is live and populated:

```
/home/johnclaw/trade-ai-state/persistent_wake/
  state/
    wakes.jsonl         36 rows   (WakeRecord@v2, all lifecycle_state=SETTLED)
    commitments.jsonl   42 rows   (CommitmentRecord@v2)
    receipts.jsonl      36 rows   (AgentConsumptionReceipt@v2, all SETTLED)
    agent_views.jsonl    9 rows   (AgentView@v1)
  research_objects.jsonl         15 rows
  research_producer_health.json   produced
```

- **36 organic wakes**, every one `provenance.trigger = schedule_slot`,
  `wake_reason = scheduled_persistent_review`, `lifecycle_state = SETTLED`.
- **36 SETTLED receipts** — the wake→commitment→receipt chain is closed on volume,
  far past the 3-contiguous-cycle M2 bar (cycle count was 1 at the 0215 stamp).
- **42 commitments**: 36 `OPEN` + 6 `FROZEN` (the outcome lifecycle is now
  exercising a terminal state beyond OPEN).

### Research → wake edge — the 0215 blocker is CLOSED

The 0215 stamp's sharpest finding was `research_objects_proxy = 0`, i.e. the
producer was merged but never scheduled. That edge is now live:

- `research_objects.jsonl` holds **15** governed research objects, producer
  `governed_research_producer`, policy `governed_brave_router` + `budget_governed`,
  `llm: null` (free-first; no LLM spend incurred).
- Producer health: `outcome = produced`, `produced = 15`, `last_success_at =
  2026-09-10T13:45:01Z`, `errors = []`.
- 11 of 36 wakes select on `unconsumed_research`; the other 25 on
  `material_change`. The research→wake selector now has real candidates and is
  no longer falling through to `material_change` every hour.

### Judgment (AgentView@v1) — organic, no longer hermetic-only

`agent_views.jsonl` holds **9** `AgentView@v1` records, produced on the scheduled
wake (15:00Z and 17:00Z), each with `citations`, `critic_pass = true`,
`mbi_behavior = 0`, `sizes_or_executes_trades = false`. Judgment is now produced
on a schedule — but every record still carries `source_sha = "unknown"`, so the
"produced on the serving SHA" clause of the FUTURE bar is unmet.

## Communication settlement

Applied forward-only in the prior wave (migration
`2026_09_07_communication_event_v2_settlement.sql`). This wave did **not**
re-verify the DB directly: a `psql` read is now classified `secret` by the guard,
so the column census from 0215 (10 settlement columns, CHECK constraints, both
indexes, `UNKNOWN_LEGACY` 708 / `SETTLED` 1) is carried forward as the last
measured value, not re-measured.

## Host

| Measure | Value |
|---|---|
| Disk | 82% used, 82G free |
| Health endpoint | `ok: true` |
| Served release | `9c3f4871c` (7 behind main) |

## Spec-vs-reality — "what the spec says the system does not do"

| Spec says | Reality |
|---|---|
| Every `AgentView@v1` is produced on the serving SHA | `source_sha = "unknown"` on all 9 |
| Every consumption receipt attests its producing SHA | `source_sha = "unknown"` on all 36 |
| The identity chain closes across main == served == dev tree | served is 7 commits behind main |
| Research producer stamps the SHA it ran under | `source_sha = "unknown"` in producer health |

## One-sentence version

The organic evidence engine is now genuinely live — 36 schedule-slot wakes, 36
settled receipts, 42 commitments, 9 AgentViews and 15 research objects all
durable in a release-independent root — but the provenance field on every record
is `source_sha = "unknown"`, and the served release lags `origin/main` by 7
commits, so identity attestation, not production, is the open wound.

## The count that matters

**36** organic wakes and **36** settled receipts — and **zero** evidence records
that name the SHA that produced them.
