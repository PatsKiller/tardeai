Status:      ACTIVE
as_of:       2026-09-10-0215 America/New_York (2026-09-10 02:15:05 EDT(-0400))
utc:         2026-09-10T06:15:05Z
Measured at: live host truth on served release 2de99cd89-main-exact-phase2-20260910-012515
Canonical campaign path: docs/architecture/maturity_gap_closure_20260910/CIO_AS_IS_2026-09-10-0215.md
Authority:   dated reading — not a behaviour spec; hermetic ≠ OBSERVED
Campaign:    /home/johnclaw/trade-ai-campaigns/maturity-gap-closure-20260909/
MBI_BEHAVIOR: 0
Supersedes:  CIO_*_2026-09-10-0114.md (emailed 01:16 ET) and CIO_*_2026-09-09-1401.md
See also:    paired AS-IS / FUTURE / GAP / HONEST_MATURITY at stamp 2026-09-10-0215

# CIO Agent — AS-IS (as-built) — 2026-09-10-0215

**Version stamp (America/New_York):** `2026-09-10-0215` (`2026-09-10 02:15:05 EDT(-0400)`)
**UTC:** `2026-09-10T06:15:05Z`

```
LEGEND (status icons — use consistently across AS-IS / FUTURE / GAP)
  █  OBSERVED_LIVE     scheduled or live path; durable output verified on CURRENT epoch
  ◈  INTEGRATED        wired end-to-end on serving SHA (producer→store→consumer)
  ◇  DOCKED            code + schema on CURRENT; consumer or organic proof incomplete
  ▓  PARTIAL           runs, but incomplete / degraded / consumer unproven
  ░  UNWIRED           code exists and is correct; nothing calls it or consumes it
  ◌  HERMETIC_ONLY     tests/helpers PASS; must NOT be quoted as OBSERVED
  ▒  DOCUMENTATION_ONLY docs/helpers without producer+consumer+organic evidence bar
  ✗  ABSENT / DARK     never executed, no producer, or produces nothing in recorded history
  ⛔ BLOCKED           structural/policy blocker (grant scope, secret, stuck checkpoint, etc.)
  M5_CANDIDATE         scheduled + unattended proven; durability OBSERVED clause still open
```

## Pin / SHA context (authoritative)

For the first time in this campaign the identity chain closes across **all three**
surfaces, including the tree that actually executes batch work.

| Surface | Value |
|---|---|
| `origin/main` | `2de99cd8965ee5ae503d9f278e997fe5902d5792` |
| Served release | `2de99cd89-main-exact-phase2-20260910-012515` |
| `SOURCE_COMMIT` / `BUILD_SHA` | `2de99cd8965ee5ae503d9f278e997fe5902d5792` |
| **Executing dev tree** | `2de99cd8965ee5ae503d9f278e997fe5902d5792` |
| Chain closes? | **YES** — all four equal |

Why the fourth row is new. `/home/johnclaw/trade-ai-v12-rebuild/trade-ai-v12-rebuild`
is what `PROJ` in crontab points at: **455 cron entries** and the batch fleet run
from it, not from the release. It was **66 commits behind `origin/main`** and 0
ahead, so every merged-and-deployed fix in this campaign was absent from the code
that actually ran on a schedule. Reconciled this wave to `2de99cd89` after proving
0 commits ahead, 24/25 staged docs byte-identical to main, the 1 divergent file a
superseded older draft, and both live-config files unchanged in the range.

## Persistent wake — the fork is closed

The wake state root is now **release-independent**:

```
/home/johnclaw/trade-ai-state/persistent_wake/state/
  wakes.jsonl        3 rows   slot(s): 2026-09-10T06:00Z
  commitments.jsonl  3 rows
  receipts.jsonl     3 rows
```

Created at **06:00:06Z** by the first wake to run under the new resolver. That path
sits outside every release tree, so a promote can no longer strand it.

All 3 wake records carry `provenance.trigger="schedule_slot"` — organic under the
strict definition — with `effect_kind="changed_question"` (not `none`, so they are
admissible consumption evidence under INTERFACE_CONTRACTS.md §6) and a complete
chain `WakeRecord@v2` → `CommitmentRecord@v2` → `AgentConsumptionReceipt@v2`, each
`lifecycle_state=SETTLED`.

**This is cycle 1 of the 3 contiguous organic cycles the M2 bar requires.**

### What the fork cost (measured, not estimated)

| Measure | Value |
|---|---|
| Distinct `wake_id` in the UNION of all release trees | **120** |
| Distinct `wake_id` in the largest single release | **114** |
| Organic wakes reachable from no current surface | **6** |

Those 6 are real `schedule_slot` wakes stranded in retired releases. They are the
concrete cost of the fork and the structural reason the three-cycle bar was
unreachable across any promote. They are **not** merged into the shared root:
AGENTS.md §0 rule 5 forbids auto-remediating divergent copies of an authoritative
store. The census and an operator proposal are at
`evidence/STATE_ROOT_FORK_DIVERGENCE_20260910T0600Z.md`.

## Communication settlement — migration applied

`migrations/2026_09_07_communication_event_v2_settlement.sql` is **applied**, and
completely, not partially: all 10 settlement columns present on
`communication_events` (62 columns total), both CHECK constraints, and both indexes
(`communication_events_settlement_idx`, `communication_events_provider_msg_uq`).

| `provider_settlement_state` | rows | `delivery_owner` |
|---|---|---|
| `UNKNOWN_LEGACY` | 708 | `legacy` |
| `SETTLED` | 1 | `gateway` |
| `NULL` | 6 | `NULL` |

Forward-only was honoured: no historical row was backfilled with invented
settlement identity. Closing the defect on paper by inventing identity would have
made the ledger less truthful, which INTERFACE_CONTRACTS.md §5 forbids.

## Research → wake edge — implemented and inert

This is the sharpest remaining AS-IS finding. The hourly selection feed reports,
on **every single pass**:

```
{"counts": {"material_changes": 86, "receipts": 62, "research_objects_proxy": 0}}
```

`research_objects_proxy` is `0`. Consequently `wake_subject_selector` has no
research candidates and **every** wake falls through to
`selection_source="material_change"` — visible in every record in
`logs/persistent_wake.log`.

Root cause: `scripts/lib/governed_research_producer.py` (merged, 12 tests) is a
**library**. It has no entrypoint, **0 cron references**, and its feature flag
`GOVERNED_RESEARCH_PRODUCER_ENABLED` defaults OFF. `BRAVE_ROUTER_LIVE` is also unset.
The canonical recurring producer does not recur.

## Host

| Measure | Value |
|---|---|
| Disk | 81% used, 88G free |
| Crontab entries | 455 |
| Wake schedule | hourly, top of hour, firing and settling |

## One-sentence AS-IS

The identity chain now closes across main, the served release **and the executing
tree**; persistent wake writes organic, fork-free evidence to a release-independent
root with a complete wake→commitment→receipt chain; settlement is migrated
forward-only; and the research→wake edge remains dark because its producer, though
merged and tested, is never scheduled.
