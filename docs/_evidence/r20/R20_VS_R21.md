# R20 live specialist status vs R21 admin visibility

Do not confuse these.

| Plane | Question | Current local truth |
|---|---|---|
| R20 specialist-office activation | Is the office runtime actually waking specialists on CURRENT? | AUDIT_COMPLETE. R19 office code is on `feat/r19-specialist-office-activation`, **not** CURRENT (`6088254e`). Not LIVE. |
| R21 admin visibility | Can the control plane *display* agent runtime_state including CALLABLE_ONLY? | Yes, once a registry exists. Displaying CALLABLE_ONLY is correct **before** R20 is live. |

R21 must never cause the UI to say LIVE merely because `/api/v3/control-plane/*` exists.
`_system().runtime.state` is currently `UNKNOWN`. That is honest.

Fleet DESIGNED expected-idle ≠ office LIVE.
