# Gain Guardian (Holdings Exit Intelligence) — Phase 0 Diagnosis — 2026-07-16

Read-only diagnosis. All items verified against live code + live DB (user `trade_ai` via .env; role `johnclaw` does not exist). No code written, no tables created. Note: another session is mid-edit on `scripts/lib/research_intelligence*.py` (RI v3 stub gate + lane filters landed at 45bfa56b; Workstream E staged-idea lifecycle NOT landed) — findings on that file reflect the tree as read today.

## Iron-rule sanity
- `data/portfolios/state/holdings.json`: **41 holdings, total_value $1,268,532.67** — inside the $1.0M–$1.4M band, count > 0. PASS.

| # | Item | Expected | Actual (live) | Adapt / Flag |
|---|------|----------|---------------|--------------|
| 0.1 | Holdings state fields | per-position basis + acquisition dates + account | 41 rows (37 non-cash). First-row keys lack basis (cash row), but **union of keys includes `cost_basis`, `avg_cost`, `cost_basis_source`, `gain_loss`, `gain_loss_pct`, `basis_partial`, `_basis_divergence_flagged`** — 35/41 rows (35/37 non-cash) have `cost_basis`. **NO acquisition-date field of any kind.** `account` values: `schwab_taxable` (13), `schwab_roth` (3), `schwab_rollover_ira` (14), `fidelity_rollover_ira` (11); `account_id` also present | adapt basis/account from holdings.json; **FLAG: no acquisition dates in holdings.json** |
| 0.2 | Tax lots `schwab_cost_basis_lots` | exists, dated lots, good coverage | Table exists. Date column is **`opened_date`** (plus `closed_date`, `term`, `wash_sale`). But: **24 rows total, 23 distinct symbols, ALL `kind='unrealized'`, 0 rows with non-null `opened_date`**, last `imported_at` 2026-06-10, accounts only `schwab_roth_ira` + `schwab_taxable` (no rollover IRAs, no Fidelity). Coverage of current book: **11/29 held non-cash symbols (37.9%)** = BAH CACI CSWC DIV LDOS NOC PFLT RTX SCHD SCHG V ≈ **58.4% of non-cash MV** — but **0% of anything is lot-DATED**. 12 lot symbols no longer held (stale) | **FLAG (hard): opened_date is 100% NULL and table is stale/partial — cannot seed HWMs or holding-period/term math from lots.** HWM must seed from daily bars since a proxy start (e.g. bar history window or first-seen), and LT/ST term is UNKNOWN — surface as "term unverified" |
| 0.3 | Protection advisor helpers | importable bar/technical helpers | `scripts/holding_protection_advisor.py` (519 lines) has **module-level, directly importable**: `_bars(symbol, days=70)` (Schwab `get_price_history` → yfinance fallback → `holding_proxies.HOLDING_PROXY_MAP` proxy bars), `_technicals(bars)` → {rsi, atr, swing_low, sma50, price}, `_analyst(cur, symbol)`, `_sanity_check(rec, t, bounds)`, `_lifecycle_stage(symbol)`, `_candidates(limit)` (holdings.json filter: real accounts, >$100, no unstoppable funds). Family logic lives in `scripts/holding_family.py` (`classify_family`, `protection_bounds`, `trail_pnl_threshold`, `volatility_tier`, `current_regime`, `is_unstoppable_fund`) | adapt: **import directly, zero lines need to move** (all helpers are top-level; only the LLM loop is inside `run()`) |
| 0.4 | RI schema + advisory insert | insert conventions | `hermes_research_intelligence`: required NOT-NULLs = source, hermes_agent_name, research_type, topic, summary, evidence_json, freshness_date, model_used, status (default `'staged'`); also symbol, thesis, thesis_type, confidence_score, prompt_hash, category_* cols, tags[]. Advisor insert (line 468): `('hermes','protection_advisor','protection_advisory', sym, topic, summary, thesis, 'neutral', evidence_json, confidence, model, PROMPT_VERSION, CURRENT_DATE)` — status left to default 'staged'. Live protection_advisory statuses: promoted 1,171 / archived 557 | adapt: clone insert shape with `research_type='exit_intelligence'`, agent name `gain_guardian` |
| 0.5 | `_TYPE_TO_CAT` mapping | exists; where to add | Exists at `scripts/lib/research_intelligence.py:87-97`. Entries: topic_research→None, momentum_catalyst→catalyst_event, **protection_advisory→risk_regime, stop_curation→risk_regime, stop_health→risk_regime**, ticker_thesis_challenge/options_desk/research_backlog→company_ticker, youtube_discovery→academic_pro. `'exit_intelligence': 'risk_regime'` slots in beside protection_advisory (map consumed at lines 158 and 187) | adapt — **coordinate with the other session editing this file**; one-line map addition |
| 0.6 | Phase 191 patterns | shadow-thresholds script + outcomes table | `scripts/profit_protection_shadow_thresholds.py` exists (docstring says Phase 206; graft_verdict taxonomy DO_NOT_GRAFT/ELIGIBLE/REJECTED; MIN_SAMPLE=20; confidence keyed to reliable-n; write-only-with `--apply`; .env loader + psycopg2 `db()` pattern). `protection_advisory_outcomes` exists (64 rows): trade_id PK-ish, advisory_existed/advisory_action, stop_before/after, operator_decision, entry/exit/current price, realized/unrealized pnl, r_multiple, mfe_raw/mae_raw, profit_locked_by_adjustment, giveback_avoided, gave_back_profit, profit_left_on_table_pct | adapt: clone both patterns (advisory-only verdict taxonomy + outcomes-table shape) for exit-intel reconciler |
| 0.7 | Chart stack | mplfinance in venv, output dir | **mplfinance 0.12.10b0 + matplotlib 3.10.8 importable in .venv.** No chart/PNG dir exists under data/runtime (only a stray e2e screenshot `data/runtime/e2e-stop-mgmt-fidelity-sync-fail.png`) | adapt: create e.g. `data/runtime/gain_guardian_charts/` at runtime (gitignored per release-hygiene rule) |
| 0.8 | RVOL / volume source | 20d avg volume available | `holding_protection_advisor.py` uses **NO volume anywhere**; `scripts/fund_technicals_enrich.py` exists but also has no volume. Existing rvol usage is Finviz screener-row fields (`rvol`/`relative_volume`) in scalp libs — screener-side, not holdings-side. **Primary transport DOES carry volume**: `schwab_transport.get_price_history` returns `{open,high,low,close,volume,datetime}` (line ~792). BUT the advisor's `_bars()` yfinance fallback and proxy paths construct OHLC-only dicts and **drop volume** | adapt: compute 20d avg vol + RVOL from `_bars()` output, `volume` key fail-soft (missing on fallback/proxy/mutual-fund bars → RVOL n/a) |
| 0.9 | Cron neighborhood | 17:05 advisor exists; 17:35 free | 17:05 advisor confirmed (crontab line 515, `flock /tmp/protection_advisor.lock`). Weekday 17:00–18:00 minutes taken: :00 (TCA, hermes_self_tune), :05 (protection advisor, strategy audits), :10 (options paper monitor), :20 Mon (grok sweep), :25 (plan_drift_reval), :30 (screener_pm, afterhours prep), :35 (**entry_planner, snaptrade_sync, stop_drift_alert**), :45 (entry_planner proposals). **17:35 is NOT free — three jobs already run there.** Free weekday minutes: **:15, :40, :50, :55**. Sunday taken: 7:30, 8:00×3, 9:30, 10:00×2, 10:30×2, 11:00, 12:00, 17:00, 19:00, 21:00, 22:00, 23:00 — e.g. **Sun 9:00 or 11:30 free** for a weekly reconciler | **FLAG: prompt's 17:35 assumption wrong** — recommend 17:40 (after advisor + stop_drift so exit intel can read today's advisory) |
| 0.10a | personal_situation.json | exists | Exists. Top-level keys: `fields`, `generated_at`, `owner`, `schema_version`. `fields` = 34 tax/disability/Roth keys (age, current_tax_bracket_pct, filing_status, golden_window_open/close, next_bracket_ceiling, roth_conversion_ytd/remaining_2026, ssdi_annual, …). Values not dumped | adapt (read-only input for tax notes) |
| 0.10b | Alex retirement helpers | importable | `scripts/alex_retirement_advisor.py` — module-level: `get_tax_context(tax_year=2026) -> dict` (line 1041), `_format_tax_context()` (241), `roth_conversion_analysis()` (567), `get_disability_context_for_prompt()` (107), `_load_personal()` (553), `analyze_for_retirement(symbol, trigger)` (427), `scan_portfolio_for_alerts(send_telegram=False)` (821) | adapt: import `get_tax_context` + `_load_personal` for taxable-account exit tax notes |
| 0.10c | Telegram dispatcher | shared module | Canonical: `scripts/telegram_alert.py` → `send_telegram(message: str, bypass_router: bool=False) -> bool` — routes through `telegram_alert_router.should_send_telegram/mark_sent/classify_alert` unless bypassed; used by `stop_drift_alert.py` (closest analog advisory). Cross-run dedupe: `scripts/telegram_alert_dedupe.py` → `dedupe_key(alert_type, component)` + `should_suppress(state, key, window_min)`, state at `data/state/alert_dedupe_state.json` | adapt: `send_telegram()` never bypass; dedupe key `('gain_guardian', symbol)` via telegram_alert_dedupe |
| 0.10d | New tables absent | should NOT exist | `holding_high_water_marks` and `holding_exit_metrics`: **neither exists** (confirmed `\d` "did not find any relation") | clean namespace — adapt |

## Flag-backs (require plan adjustment or operator awareness)
1. **Lot dates are unusable**: `schwab_cost_basis_lots.opened_date` is NULL on all 24 rows, all rows `kind='unrealized'`, import stale since 2026-06-10, and only 2 of 4 accounts represented. **% of book with lot-DATED coverage for HWM seeding = 0%** (symbol-level lot coverage is 37.9% of held symbols / 58.4% of non-cash MV, but none dated). HWM seeding must come from daily bar history (e.g. max close over the bar window) with an explicit "seeded-from-bars, not purchase-anchored" marker; LT/ST term cannot be verified — label, don't guess.
2. **holdings.json has no acquisition dates** either — same consequence as (1); it DOES have per-position `cost_basis`/`avg_cost` on 35/37 non-cash rows (2 rows without are the delisted/CUSIP oddities), so gain-% math is fine.
3. **17:35 cron slot is occupied** by entry_planner + snaptrade_sync + stop_drift_alert. Use 17:40 (or :15/:50/:55).
4. **Volume is lost on `_bars()` fallback paths** (yfinance + proxy construct OHLC-only dicts) — RVOL must be fail-soft; primary Schwab path has per-bar volume.
5. **`scripts/lib/research_intelligence.py` is being edited by another session** — the one-line `_TYPE_TO_CAT` addition (`'exit_intelligence': 'risk_regime'`) must be coordinated/rebased, not raced (shared-tree git hazard rule).
6. Cosmetic: `profit_protection_shadow_thresholds.py` self-identifies as **Phase 206**, not 191 — pattern is the right one regardless.

## Import map for the build (no refactors needed)
- Bars/technicals: `scripts/holding_protection_advisor.py` → `_bars`, `_technicals`, `_candidates`, `_sanity_check`, `_lifecycle_stage`, `_analyst` (all module-level).
- Family/regime: `scripts/holding_family.py` → `classify_family`, `protection_bounds`, `trail_pnl_threshold`, `volatility_tier`, `current_regime`, `is_unstoppable_fund`.
- Transport (if bypassing advisor helper for volume): `scripts/schwab_transport.py` → `get_price_history(symbol, start, end, timeframe="1Day")` (OHLCV dicts).
- Telegram: `scripts/telegram_alert.py` → `send_telegram`; `scripts/telegram_alert_dedupe.py` → `dedupe_key`/`should_suppress`/`record_sent`.
- Tax notes: `scripts/alex_retirement_advisor.py` → `get_tax_context(tax_year)`, `_load_personal()`; raw file `data/portfolios/state/personal_situation.json` (fields dict).
- RI insert: clone `holding_protection_advisor.py:468` INSERT (status default 'staged'), `research_type='exit_intelligence'`.
- DB/env: `.env` loader + psycopg2 pattern per `profit_protection_shadow_thresholds.py` `load_env()`/`db()`, or `db_adapter._get_conn` as in the advisor.
