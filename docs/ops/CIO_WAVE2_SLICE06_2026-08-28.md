# CIO Wave 2 Slice 06 — earnings days_to_event + as_of

Date: 2026-08-28
Authority: READ_ONLY_ADVISORY

Each earnings item now includes `days_to_event` (from source date) and keeps
`as_of` / `source_as_of` (fetched_at when present). Empty only if file missing.
