# Stage 0 Baseline

Date: 2026-07-28

START SHA: `a51ddd72f75b9fbe5dac749bc786396c9b558104`

Requested branch: `feat/telegram-notification-normalization`

Actual branch during implementation: `main`

Branch creation result: blocked by sandbox; `.git/refs/heads/...lock` could not be created because `.git` is read-only.

Initial working tree:

- Modified unrelated file: `config/ipo_lockups.json`
- Untracked existing artifacts: `apps/command-center-v3/dist.old-*`, `docs/_findings/at_config_tab_recon_2026-07-28.md`, and the alert evidence package directory

Active AGENTS.md instructions:

- No applicable repository `AGENTS.md` found outside `node_modules`.

Database migration framework:

- Primary migrations are additive SQL files under `migrations/`.
- Rollback files are paired manually when needed.
- Separate `migrations/agentic_runtime/*.up.sql/*.down.sql` exist for agentic runtime only and are applied through `migrations/agentic_runtime/apply.sh`.

Current test commands used:

- `.venv/bin/python -m pytest tests/test_alert_fatigue_1_routing.py tests/test_telegram_alert_router_jun25.py tests/test_telegram_notification_normalization.py -q`
- `python3 -m py_compile <changed scripts>`
- `cd apps/command-center-v3 && npm run build`
- `.venv/bin/python scripts/evaluate_telegram_notification_normalization.py <csv> --json`

Current v3 Reports and System routes:

- SPA routes preserved: `/v3/reports`, `/v3/system`, `/v3/go/order/:intentId`, `/v3/go/proposal/:proposalId`
- New additive route target: `/v3/go/alert/<alert_id>` generated by `notification_url_builder.build_alert_url`
- Existing Reports APIs preserved: `/api/v2/reports/*`
- New additive alert APIs: `/api/v3/alerts/active`, `/api/v3/alerts/settings`, `/api/v3/alerts/settings/preview`, `/api/v3/alerts/test-send`

Safety baseline:

- Real order queued/submitted/modified/cancelled: NO
- Real 2FA requested: NO
- Real Telegram sent: NO
- Production secret read/printed: NO
- Broker write: NO
- Production deployment: NO
