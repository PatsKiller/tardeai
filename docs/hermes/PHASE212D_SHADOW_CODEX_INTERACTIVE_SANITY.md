# PHASE 212D — Shadow Codex Interactive Sanity (2026-06-07)
N/A (no shadow build — see 212C). Production interactive Codex confirmed working separately:
`hermes -p dev chat` with provider=openai-codex / gpt-5-codex (subscription-backed, free). Prod profiles
intact (default/tradeai/tradeai12b/dev/serverops); ~/.hermes untouched; OAuth creds preserved.
