# R8 — Empirical Factor / Strategy Families

Fixture-first empirical reproduction. No confirmatory claim without a frozen
complete trial family that records losers.

- Freeze family → record every planned variant → multiple-testing + family RC
- Winner-only selection fails closed
- OOS requires a complete frozen family; consumed OOS cannot be relabeled fresh
- Grade ceiling and `CONTEXT_MODIFIER` unless the RG ladder actually passes
- Uses `tests/fixtures/us_equity_monthly_sample.csv` (not a vendor print)

Authority: `READ_ONLY_ADVISORY`. Module: `scripts/lib/research_governance/empirical.py`
