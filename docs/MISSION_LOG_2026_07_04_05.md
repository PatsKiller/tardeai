# Mission Log — 2026-07-04/05 (Due Diligence · Discovery Inbox · Incident Sweep)

21 merged PRs (#90–#99, #101–#110 + flap-window chore). All phases complete.

## Due-diligence mission (Phases 0–6) — ✅ all done
- **Phase 0** — full repo discovery + **Dry Test 1** (expected-vs-found recorded; stale premises documented).
- **Phase 1** — secrets audit + **Dry Test 2**: repo found PUBLIC → flipped private; tracked Google-cookie backup untracked; PII scrubbed (acct → ***199); `.gitignore` hardened (PR #94); **git history purged** via git-filter-repo (3,128 commits rewritten; exposed Anthropic-key file, cookie generations, DB-password literal, holdings snapshots removed from all remote history; rollback bundle in `~/backups/`). Residual: GitHub `refs/pull/*` pin pre-purge SHAs until a Support GC request; operator owns key rotation + Google sign-out.
- **Phase 2** — Propose Entry modal **verified PASS** (no changes needed): cash-based sizing on all 4 accounts incl. Fidelity, retirement no-margin (client- AND server-side), 1%/2% toggle, graduated warnings, auto-size; live UI math matched hand-trace exactly; 12 unit tests.
- **Phase 3** — Security Card **v4 family designed, built, parity-gated, LOCKED** for all surfaces (watchlist, broker proposals, open-trades positions, options desk; PRs #95–#97) behind global `cc.cards.v4` (default ON). Two operator feedback rounds folded in (#101, #102, #104, #105).
- **Phase 4** — intelligence consistency: market-aware weekend staleness on cards, DD report links verified (PDF/Word/Generate), catalyst-age signaling, LLM stances.
- **Phase 5** — top-level `ARCHITECTURE.md` + `OPERATIONS.md` authored (PR #99, no-hallucination verified); reference-architecture docx appended + Drive-synced.
- **Phase 6** — **Dry Test 3 PASS** + Pre-Private Readiness Checklist delivered; full-suite dry run recorded (1482 pass / 69 pre-existing isolation failures — triage plan in memory).

## Discovery Inbox (Parts A–L) — ✅ shipped & live (PR #110)
Governed discover→review→promote bridge: 4 additive tables (audited state machine, 14 statuses), `scripts/lib/hermes_discovery/` (dedupe clustering, transparent scoring with parts, symbol-validation gate that quarantines shape-accepted junk, bounded feedback), 4 producer ingestors, promotion module with **import-time forbidden-path guard** (no broker imports; only 4 legal pathways, all via existing app-role machinery), 10 advisory-only API routes, scorecard v2 + do-no-harm (tighten/pause), Hermes-hub Discovery tab (v4 design, advisory banner on every card). 29 tests green. Status: **automation ON · autonomous promotion OFF · learning: collecting evidence**.

## Incident/alert workstreams — ✅ all closed
- **Idle-in-transaction class eliminated** across 9 scripts (PRs #90–#93 + classify_instruments + discovery feedback path): commit-before-slow-work is now doctrine (`docs/runbooks/DB_HANG_PREVENTION.md`).
- **Card intelligence gaps** (PR #92): descriptions, real Finviz scores, ATM-lane agent reviews (PENDING-only filters), options underlying context; 180 unclassified instruments backfilled.
- **LLM review queue**: stage-2b worker fixed (8th idle-txn offender) + was UNSCHEDULED → cron added; 364 stale rows expired; queue drained to zero.
- **Alert noise — all four causes dead**: duplicate user-timer (deleted), status flap (guard + 360-min window), NYSE-holiday-blind staleness (PR #109), SIEM re-page loop (24h cooldown + escalation triggers).
- **tax_agent**: producer retired without rehoming + auto-queue remediation broken for a month (invalid SQL + missing id + aborted-txn leak) — all fixed (PR #108), weekly sweep cron restored, freshness 0d.
- **Sizing interactivity** (PRs #102/#104/#105): liquidity status words, visible equity-risk, provenance line, clickable volatility suggestion (0.5/0.75% flow through to the modal), always-actionable size with stale caution, resize-to-policy hint on account switch.
