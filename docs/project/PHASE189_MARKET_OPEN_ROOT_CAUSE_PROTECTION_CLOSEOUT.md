# PHASE 189 — Market-Open Watch + Protection Root-Cause Investigation — CLOSEOUT

Status:      HISTORICAL
as_of:       2026-06-02T09:35:38-04:00
Measured at: efcc51365 / not measured

**Run:** 2026-06-02 ~09:10 ET (premarket; watch fires 09:30 ET)
**Scope:** Alpaca **paper** only. Investigation + scheduling + docs. **No trades, no stop
changes, no strategy-config changes, no GO/WAIT changes, no live anything.**

---

## Headline (and a correction to Phase 188)
**Phase 188 said ANY/SNOW were "naked." That was wrong.** Read-only verification against the
Alpaca **paper** order book shows **all 6 open positions hold a live broker stop** (ANY @3.07,
SNOW @254.38, TMHC @68.02, NWG @15.05, CMCSA @23.61, AGNC @9.71). The real defect is a
**record/verify/tracking + alert-routing gap**: three positions (the two `alpaca_sync` ones and
the market-order TMHC) have stops the DB never recorded, no scheduled verifier persists them, and
the one monitor that detects the gap log-swallows it with no alert — while the alert-capable
health agents inspect brokerage JSON instead of `paper_trades`.

## Sub-phase outputs
- `docs/atm/PHASE189A_MARKET_OPEN_WATCH_SCHEDULE_REPORT.md`
- `docs/atm/PHASE189B_EXTENDED_HOURS_BLOCKAGE_ROOT_CAUSE.md`
- `docs/atm/PHASE189C_MISSING_STOP_ROOT_CAUSE_TRACE.md`
- `docs/ops/PHASE189D_HEALTH_AGENT_MISSING_STOP_FAILURE_ANALYSIS.md`
- `docs/hermes/PHASE189E_HERMES_MISSING_STOP_FAILURE_ANALYSIS.md`
- `docs/atm/PHASE189F_MARKET_OPEN_REVALIDATION_REPORT.md` (auto-generated by the watch; premarket
  dry-run committed, refreshed live at 09:30 ET)
- `docs/atm/PHASE189G_DURABLE_PROTECTION_REMEDIATION_PLAN.md`
- `scripts/atm_market_open_watch.py` (read-only watch)

## POST-OPEN UPDATE (09:32 ET — watch fired)

The 09:30 watch fired and live data is in. Authoritative post-open results (see refreshed 189F/189E):
- **Watch fired:** ✅ YES (09:30:02 ET).
- **ELMT final 09:30 status:** **REJECTED** — auto-rejected 09:00 ET premarket (`auto_blocked_230min`,
  aged out). Fresh quote now but ~31% open spread. **Submitted: NO.** No new ELMT proposal post-open.
- **ANY:** fresh quote YES; live **+$569.48 (+5.75R)**; broker stop **YES** (@3.07); DB stop tracked **NO**.
- **SNOW:** fresh quote YES; prior +18% mark was **STALE** → live ≈ **+13.3% (+$251.50)**; broker stop
  **YES** (@254.38, locks gain); DB stop tracked **NO**.
- **TMHC:** broker stop verified **YES** (@68.02); DB stop tracked **NO**; note not broker-confirmed.
- **Naked broker stop count:** 0 · **Untracked broker stop count:** 3 · **Take-profit missing:** 6.
- **Telegram sent:** NO (corrected payload prepared in 189E; awaiting operator OK).
- **No new trades** premarket/at open.

## Required closeout fields
- **Phase 189 complete:** ✅ YES (investigation + schedule + watch-fired + docs)
- **Watch scheduled:** ✅ YES — cron `atm-market-open-watch-0930`, 09:30 ET today
- **Watch fired:** ✅ **YES (09:30:02 ET)**
- **ELMT status:** **REJECTED** (auto_blocked_230min, aged out premarket; ~31% open spread). Not
  submitted. Dedup works (same row re-evaluated). No `PENDING_TRADING_WINDOW` state exists — gap.
- **Extended-hours blockage root cause:** `market_session.should_delay_execution` returns
  `delayed` for the `premarket` session inside `paper_execution_revalidator.revalidate`
  (`market_session.py:135-137`, enforced `proposal_paper_submitter.py:558`), short-circuiting
  before the extended-hours-capable adapter runs; compounded by the default **IEX** data feed
  returning no live premarket quote. NOT cron, NOT order type, NOT a missing param.
- **ANY root cause:** ALPACA_SYNC_ONBOARDED_WITHOUT_METADATA + IMPORTED_POSITION_WITHOUT_PROTECTION
  + STOP_PLACED_BUT_NOT_RECORDED + HEALTH_AGENT_COVERAGE_GAP (broker stop @3.07 exists, unrecorded)
- **SNOW root cause:** same four labels (broker stop @254.38 exists, unrecorded; mark was stale)
- **TMHC root cause:** STOP_PLACED_BUT_NOT_RECORDED + JOURNAL_METADATA_GAP + HEALTH_AGENT_COVERAGE_GAP
  (proposal 159 carried stop 68.02; broker stop exists; `_api_post` return discarded at
  `alpaca_paper_adapter.py:534`; note written from `use_market` boolean, not broker confirmation)
- **Health-agent failure root cause:** **alert-routing bug + coverage gap.** Detection ran every
  3 min and correctly flagged CRITICAL, but `unified_stop_supervisor.py:126-128` log-swallows it
  with no Telegram/SIEM; the alert-capable `system_health_agent.py`/`aegis` read brokerage
  `risk_management.json`, not `paper_trades`. No "large gain + no stop" rule exists.
- **Hermes failure root cause:** no safe view exposes broker-protection fields; no rule for
  naked/unverified/unprotected positions; the only position-touching loop
  (`hermes_autonomous_loop.py:65`) queries CLOSED trades only.
- **Open positions reviewed:** **6**
- **Naked positions count (no broker stop):** **0**
- **Unverified broker stops count (stop exists, DB `stop_order_id` missing):** **3** (ANY, SNOW, TMHC)
- **Take-profit missing count:** **6**
- **Health-agent failure confirmed:** ✅ YES — detection ran, CRITICAL log-swallowed, no SIEM/Telegram
- **Hermes safe-view gap confirmed:** ✅ YES — no view exposes broker-protection fields; no rule
- **Phase 190 required:** ✅ YES
- **Telegram sent:** **NO** (corrected payload in 189E; not auto-sent — awaiting operator OK)
- **Durable remediation plan ready:** ✅ YES (189G)
- **Paper account verified:** ✅ YES (`PA3E93QWASV1`, ACTIVE, `ALPACA_MODE=paper`)
- **Alpaca broker verified:** ✅ YES (paper endpoint, read-only)
- **Live endpoint blocked:** ✅ YES
- **Live trading:** **ZERO**
- **GO/WAIT mutation:** **ZERO**
- **Strategy mutation:** **ZERO** (pre-existing dirty `config/strategies/*.yaml` left untouched)
- **Level 7:** **PROHIBITED**
- **Next recommended gate:** **PHASE 190 — Durable Protection Guardrails Implementation.** Make
  protection provable/verified/alertable (capture & persist `stop_order_id` + `stop_verified_at`;
  schedule the broker-stop verifier; wire CRITICAL → SIEM+Telegram; point health checks at
  `paper_trades`; add Hermes protection view+rule; add `PENDING_TRADING_WINDOW`). Do **not**
  "place stops" — the stops already exist.

## Guardrail attestation
No live account, no live endpoint, no live broker mode, no live trades, no holdings mutated, no
strategy configs changed, no GO/WAIT/NO-GO logic changed, Level 7 not enabled, Claude Code
auto-update not run. The only mutations performed were: (1) adding a read-only watch script, (2)
adding a date-pinned one-shot cron entry (rollback documented in 189A), (3) writing docs.
