# ROCKVILLE_WATCH_UI_SCREENSHOTS

Placeholder for acceptance screenshots.

Required captures (when `watch_card_v2_visible` or shadow band on):

1. `/v3/watch?tab=watchlist` — route + active Watchlist tab asserted before capture  
2. CIO Daily panel (or empty shadow state)  
3. FTH card: DETERMINISTIC FAIL banner, no trigger/invalidation, synthesis panel visible  
4. Tablet width (~768px)  

Commands (operator):

```bash
# after enabling shadow and starting portfolio-server
# use existing CC screenshot harness if present
```
