# Hermes Sidecar Config — Trade AI v12
# Phase 1: Local Ollama only, no external APIs, tool-use disabled
model:
  provider: custom
  default: gemma3:12b
  base_url: http://127.0.0.1:11434/v1
  api_mode: openai
  disable_tools: true

terminal:
  backend: local
  cwd: /home/johnclaw/trade-ai-v12-rebuild/trade-ai-v12-rebuild
  timeout: 180
  env_passthrough:
    - OLLAMA_HOST

memory:
  provider: file
