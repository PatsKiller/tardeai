# Operator-documented authoritative cost basis for long-held lots whose opening predates the Schwab API
# transaction window (2025-07-19). Used by schwab_journal_builder when no in-window or CSV opening lot
# exists. NEVER reconstructed from the truncated API window. Split-adjusted per-share basis.
# Format:  "SYMBOL|account": basis_per_share
overrides:
  "V|schwab_roth_ira": 10.75   # 100 sh 2008 IPO @ ~$43 -> 4:1 split (2015) -> 400 sh @ ~$10.75; long-term winner
