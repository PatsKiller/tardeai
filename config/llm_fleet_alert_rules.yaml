# Embedding-only GPU observability. Local generation is forbidden.
phase: embedding_only_transition
enabled: true
expected_resident_models:
  outside_deep_window:
    - nomic-embed-text
  transient_allowed: []
thresholds:
  min_free_vram_gb_warn: 1.0
  nomic_latency_ms_warn: 1000
  failure_rate_warn_pct: 5
  forbidden_model_count_critical: 1
