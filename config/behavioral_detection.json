{
  "_meta": {
    "version": "1.0.0",
    "created_at": "2026-08-09",
    "status": "SHADOW",
    "note": "Behavioral finance detection thresholds. Shadow-only — findings are advisory, never execute. Operator sees through Alex. Thresholds tuned for ~$1.27M book."
  },
  "disposition_rule1": {
    "enabled": true,
    "description": "Long-held material losers — primary 'hold losers too long' signal",
    "min_loss_pct": 0.15,
    "min_loss_abs": 8000,
    "min_holding_months": 6,
    "min_weight_pct": 0.025,
    "critical_loss_pct": 0.20,
    "critical_loss_abs": 20000,
    "critical_holding_months": 3,
    "thesis_stale_months": 6,
    "severity": {
      "medium": { "min_loss_pct": 0.15, "min_holding_months": 6, "min_loss_abs": 5000 },
      "high": { "min_loss_pct": 0.20, "min_holding_months": 4, "min_loss_abs": 10000 },
      "critical": { "min_loss_pct": 0.20, "min_holding_months": 3, "min_loss_abs_override": 20000 }
    }
  },
  "disposition_rule2": {
    "enabled": false,
    "description": "Rapid realization of winners while material losers remain — requires realization history",
    "note": "Disabled until clean realization history is available from broker transaction log"
  },
  "false_positive_mitigations": {
    "exclude_if_thesis_active": true,
    "exclude_if_core_holding": true,
    "exclude_if_tax_constrained": true,
    "exclude_if_guardian_risk_signal": true,
    "exclude_if_loss_below_min_abs": true
  }
}
