# Fail-closed jailbreak/instruction-override scan at memory admission.
# Default in code is 0 (parity). Enable on serve after tests pass.
# Does NOT set MEMORY_BEHAVIOR_INFLUENCE.
[Service]
Environment=MEMORY_ADVERSARIAL_SCAN=1
Environment=MEMORY_BEHAVIOR_INFLUENCE=0
