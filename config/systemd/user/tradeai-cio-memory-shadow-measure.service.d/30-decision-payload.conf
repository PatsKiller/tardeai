# Phase B.1 — measure process must see the same capture flag as producers.
# Does NOT enable influence. Empty v1 corpus still reports available=false.
[Service]
Environment=AGENT_DECISION_PAYLOAD=1
Environment=MEMORY_PROVIDER=durable
Environment=MEMORY_SHADOW=1
Environment=MEMORY_BEHAVIOR_INFLUENCE=0
