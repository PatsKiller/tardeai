# Phase B.1 — flag on. Dry-run path now emits advisory DecisionPayload@v1
# (deterministic verdicts are operator-visible decisions).
[Service]
Environment=AGENT_DECISION_PAYLOAD=1
Environment=MEMORY_BEHAVIOR_INFLUENCE=0
