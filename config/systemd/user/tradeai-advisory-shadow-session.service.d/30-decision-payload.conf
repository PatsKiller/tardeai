# Phase B.1 — flag on so emit works when the session is wired.
# Shadow session does not yet call emit_decision_payload (B.2).
[Service]
Environment=AGENT_DECISION_PAYLOAD=1
Environment=MEMORY_BEHAVIOR_INFLUENCE=0
