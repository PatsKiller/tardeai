import { useEffect, useMemo, useState } from 'react'
import { useApi } from '../hooks/useApi'
import { useProAnalystMap } from '../components/ProAnalystPill'
import { deriveTradeAiRating, normalizeAnalystRating, ratingLabel, ratingRank, type ManualTosRating } from '../lib/manualTosRating'

type SourceKind = 'PROPOSAL' | 'WATCHLIST' | 'DRAFT'
type SetupState = 'READY' | 'GENERATED' | 'BROKER_OBSERVED' | 'EXCEPTION'
type SourceFilter = 'ALL' | SourceKind
type AccountFilter = 'ALL' | 'ANY' | 'schwab_taxable' | 'schwab_rollover_ira' | 'schwab_roth_ira'
type DecisionFilter = 'ALL' | 'GO' | 'WAIT' | 'OTHER'
type AnalystFilter = 'ALL' | ManualTosRating
type StatusFilter = 'ALL' | SetupState
type SortMode = 'SCORE_DESC' | 'SYMBOL_ASC' | 'SOURCE' | 'STATUS' | 'ENTRY_PRICE' | 'ANALYST'

type LocalState = Record<string, { account?: string; qty?: number; generated?: boolean }>
type UiPrefs = { source: SourceFilter; account: AccountFilter; decision: DecisionFilter; analyst: AnalystFilter; status: StatusFilter; search: string; minScore: string; hasEntry: boolean; hasRisk: boolean; sort: SortMode }
type SetupRow = { id: string; source: SourceKind; raw: any; symbol: string; company?: string | null; analyst?: string | null; analystCount?: number | null; analystUpside?: number | null; account: string; qty: number; side: string; entryType: string; entryPrice: number | null; stop: number | null; trail: any | null; targets: any[]; tif: string; session: string; score?: number | string | null; decision?: string | null; reason?: string | null; sector?: string | null; sourceLabel?: string | null }

type AccountSnapshot = { key: string; label: string; cash: number | null; buyingPower: number | null; accountValue: number | null }

const LS_KEY = 'tradeai.manualTosDesk.v2'
const PREF_KEY = 'tradeai.manualTosDesk.prefs.v4'
const ACCOUNTS = ['ANY', 'schwab_taxable', 'schwab_rollover_ira', 'schwab_roth_ira']
const C = { blue: '#60a5fa', green: '#22c55e', amber: '#f59e0b', red: '#ef4444', purple: '#a78bfa', dim: 'var(--text3)' }
const mono = { fontFamily: 'JetBrains Mono, ui-monospace, SFMono-Regular, Menlo, monospace' as const }
const btn = (bg: string, fg = '#fff') => ({ fontSize: 10, fontWeight: 700, padding: '6px 10px', borderRadius: 6, border: 'none', background: bg, color: fg, cursor: 'pointer' as const })
const inputStyle = { fontSize: 10, padding: '6px 8px', borderRadius: 6, border: '1px solid var(--border)', background: 'var(--bg2)', color: 'var(--text0)' } as const
const metricBox = { background: 'rgba(15,23,42,.48)', border: '1px solid var(--border)', borderRadius: 7, padding: '6px 7px' } as const
const defaultPrefs: UiPrefs = { source: 'ALL', account: 'ALL', decision: 'ALL', analyst: 'ALL', status: 'ALL', search: '', minScore: '', hasEntry: false, hasRisk: false, sort: 'ANALYST' }

function num(v: any): number | null { const x = Number(v); return Number.isFinite(x) && x > 0 ? x : null }
function anyNum(...vals: any[]): number | null { for (const v of vals) { const x = Number(v); if (Number.isFinite(x) && x >= 0) return x } return null }
function first(...vals: any[]) { return vals.find(v => v !== undefined && v !== null && v !== '') }
function str(v: any) { return v == null ? '' : String(v) }
function money(v: number | null | undefined) { return v == null || !Number.isFinite(Number(v)) ? '—' : `$${Number(v).toLocaleString(undefined, { maximumFractionDigits: 0 })}` }
function pct(v: number | null | undefined) { return v == null || !Number.isFinite(Number(v)) ? '—' : `${Number(v).toFixed(2)}%` }
function scoreNum(v: any) { const n = Number(v); return Number.isFinite(n) ? n : 0 }
function acct(a: string) { return !a || a === 'ANY' ? 'ANY SCHWAB' : a.replace('schwab_', '').replace(/_/g, ' ').toUpperCase() }
function baseQty(x: any) { return Number(first(x.qty, x.shares, x.quantity?.qty, x.recommended_qty, x.position_size_shares, 10)) || 10 }
function normalizedDecision(v: any): DecisionFilter { const d = String(v ?? '').toUpperCase().replace('_', '-'); if (d === 'GO') return 'GO'; if (d === 'WAIT') return 'WAIT'; return 'OTHER' }
function decisionText(r: SetupRow) { const d = String(r.decision ?? '').toUpperCase(); if (d === 'GO' || d === 'WAIT') return d; return r.source === 'WATCHLIST' ? 'WATCHLIST ADVISORY' : 'DECISION PENDING' }
function rawAnalyst(x: any) { return first(x.analyst_rating, x.analyst_recommendation, x.analyst_consensus, x.consensus_rating, x.recommendation, x.recommendationKey, x.rating, x.finviz_analyst, x.finviz_recommendation, x.street_rating, x.wall_street_rating, x.tipranks_rating, x.zacks_rank, x.analyst) }
function analystColor(a: any) { const n = typeof a === 'string' ? normalizeAnalystRating(a) : deriveTradeAiRating(a).rating; return n === 'STRONG_BUY' ? '#22c55e' : n === 'BUY' ? '#84cc16' : n === 'HOLD' ? '#f59e0b' : n === 'SELL' || n === 'STRONG_SELL' ? '#ef4444' : C.dim }
function accountSnapshot(a: any): AccountSnapshot {
  const b = a?.balances ?? a?.currentBalances ?? a?.securitiesAccount?.currentBalances ?? a?.account?.currentBalances ?? {}
  const ib = a?.initialBalances ?? a?.securitiesAccount?.initialBalances ?? {}
  const cash = anyNum(a?.cash, a?.cash_balance, a?.available_cash, a?.cash_available, a?.cashAvailableForTrading, b?.cashBalance, b?.cashAvailableForTrading, b?.availableFunds, b?.moneyMarketFund)
  const buyingPower = anyNum(a?.buying_power, a?.buyingPower, a?.available_funds, a?.availableFunds, b?.buyingPower, b?.stockBuyingPower, b?.availableFunds, b?.cashAvailableForTrading, cash)
  const accountValue = anyNum(a?.account_value, a?.accountValue, a?.liquidation_value, a?.liquidationValue, a?.market_value, b?.liquidationValue, b?.longMarketValue, ib?.accountValue)
  return { key: a?.account_key ?? a?.key ?? a?.accountKey ?? '', label: acct(a?.account_key ?? a?.key ?? a?.accountKey ?? ''), cash, buyingPower, accountValue }
}
function sizing(r: SetupRow, account: AccountSnapshot | null) {
  const notional = r.entryPrice ? r.entryPrice * r.qty : null
  const cashBase = account?.cash ?? account?.buyingPower ?? null
  const acctBase = account?.accountValue ?? null
  const risk = r.entryPrice && r.stop && r.entryPrice > r.stop ? (r.entryPrice - r.stop) * r.qty : null
  const targetPrice = r.targets?.[0]?.price ? Number(r.targets[0].price) : null
  const reward = r.entryPrice && targetPrice && targetPrice > r.entryPrice ? (targetPrice - r.entryPrice) * r.qty : null
  const rr = risk && reward ? reward / risk : null
  return { notional, pctCash: notional && cashBase ? notional / cashBase * 100 : null, pctAccount: notional && acctBase ? notional / acctBase * 100 : null, risk, pctRiskAccount: risk && acctBase ? risk / acctBase * 100 : null, reward, rr }
}
function recFromProMap(symbol: string, paMap: Record<string, any>) { const p = paMap[symbol.toUpperCase()]; return p?.has ? p.rec : null }

function fromProposal(p: any, local: LocalState, paMap: Record<string, any>): SetupRow | null { const symbol = String(first(p.symbol, p.ticker, p.instrument?.symbol, '')).toUpperCase(); if (!symbol) return null; const id = String(first(p.proposal_id, p.id, `proposal-${symbol}-${p.created_at ?? ''}`)); const entry = num(first(p.entry_price, p.limit_price, p.suggested_entry, p.price, p.current_price)); const stop = num(first(p.stop_price, p.stop_loss, p.initial_stop)); const target = num(first(p.target_price, p.take_profit, p.target1, p.target)); const pro = paMap[symbol] ?? {}; return { id, source: 'PROPOSAL', raw: p, symbol, company: first(p.company, p.name, p.company_name), analyst: first(rawAnalyst(p), pro?.rec), analystCount: pro?.n, analystUpside: pro?.upside, account: local[id]?.account ?? 'ANY', qty: local[id]?.qty ?? baseQty(p), side: String(first(p.direction, p.side, 'BUY')).toUpperCase().includes('SHORT') ? 'SELL SHORT' : 'BUY', entryType: String(first(p.entry_type, p.order_type, 'LIMIT')).toUpperCase(), entryPrice: entry, stop, trail: null, targets: target ? [{ price: target, qty_pct: 100 }] : [], tif: 'DAY', session: 'NORMAL', score: first(p.score, p.final_score, p.rating_score), decision: first(p.decision, p.status), reason: first(p.thesis, p.catalyst, p.reason, p.strategy_name, 'Paper proposal promoted to manual ToS setup'), sector: p.sector ?? null, sourceLabel: 'paper proposal' } }
function fromTradeAiScanner(x: any, local: LocalState, paMap: Record<string, any>): SetupRow | null { const symbol = String(first(x.symbol, x.ticker, '')).toUpperCase(); if (!symbol) return null; const id = `scanner-${symbol}-${first(x.source, 'tradeai')}`; const price = num(first(x.price, x.last, x.current_price)); const pro = paMap[symbol] ?? {}; return { id, source: 'WATCHLIST', raw: x, symbol, company: first(x.company, x.name, x.company_name), analyst: first(rawAnalyst(x), pro?.rec), analystCount: pro?.n, analystUpside: pro?.upside, account: local[id]?.account ?? 'ANY', qty: local[id]?.qty ?? 10, side: 'BUY', entryType: 'LIMIT', entryPrice: price, stop: null, trail: null, targets: [], tif: 'DAY', session: 'NORMAL', score: x.score ?? null, decision: x.decision ?? null, reason: first(x.catalyst, x.reason, x.sector, 'Trade AI scanner/watchlist candidate'), sector: x.sector ?? null, sourceLabel: x.source ?? 'trade-ai-scanner' } }
function fromWatchlistItem(it: any, local: LocalState, paMap: Record<string, any>, cardMap: Record<string, any>): SetupRow | null { const symbol = String(first(it.symbol, it.ticker, '')).toUpperCase(); if (!symbol) return null; const card = cardMap[symbol] ?? {}; const pro = paMap[symbol] ?? {}; const id = `watchlist-${symbol}`; const price = num(first(it.price, it.last, it.current_price, card.price)); const score = first(it.hermes_score, it.score, it.composite_score, card.score); const rec = first(rawAnalyst(it), pro?.rec, card.analyst_rating, card.recommendation); return { id, source: 'WATCHLIST', raw: it, symbol, company: first(it.company, it.name, it.company_name, card.company, card.name), analyst: rec, analystCount: pro?.n, analystUpside: pro?.upside, account: local[id]?.account ?? 'ANY', qty: local[id]?.qty ?? 10, side: 'BUY', entryType: 'LIMIT', entryPrice: price, stop: num(first(it.entry_stop, card.entry_stop, it.stop, it.stop_loss)), trail: null, targets: num(first(it.entry_target, card.entry_target, it.target, it.target_price)) ? [{ price: num(first(it.entry_target, card.entry_target, it.target, it.target_price)), qty_pct: 100 }] : [], tif: 'DAY', session: 'NORMAL', score, decision: first(it.decision, it.trade_ai_decision, card.decision), reason: first(it.catalyst, it.reason, it.thesis, card.catalyst, card.summary, it.description, it.sector, 'Curated watchlist candidate'), sector: first(it.sector, card.sector), sourceLabel: first(it.origin_system, it.source, 'curated-watchlist') } }
function fromDraft(d: any, local: LocalState, paMap: Record<string, any>): SetupRow | null { const it = d.intent_json ?? d.intent ?? d; const symbol = String(first(d.symbol, it.instrument?.symbol, '')).toUpperCase(); if (!symbol) return null; const id = String(first(d.intent_id, it.intent_id, `draft-${symbol}-${it.entry?.limit_price ?? ''}`)); const entry = it.entry ?? {}; const fallbackQty = Number(first(it.quantity?.qty, d.qty, 10)) || 10; const pro = paMap[symbol] ?? {}; return { id, source: 'DRAFT', raw: d, symbol, company: first(d.company, it.instrument?.name, it.company_name), analyst: first(rawAnalyst(d), pro?.rec), analystCount: pro?.n, analystUpside: pro?.upside, account: local[id]?.account ?? String(first(it.account_key, d.account_key, 'ANY')), qty: local[id]?.qty ?? fallbackQty, side: it.direction === 'SHORT' ? 'SELL SHORT' : 'BUY', entryType: String(first(entry.method, 'LIMIT')).toUpperCase(), entryPrice: num(first(entry.limit_price, entry.stop_price)), stop: num(it.exit_policy?.stop?.price), trail: it.exit_policy?.stop?.trail ?? null, targets: Array.isArray(it.exit_policy?.targets) ? it.exit_policy.targets : [], tif: String(first(it.tif, 'DAY')), session: String(first(it.session, 'NORMAL')), reason: it.meta?.thesis ?? 'Broker draft setup', sourceLabel: 'draft' } }

function setupLine(r: SetupRow) { const price = r.entryPrice != null ? ` ${r.entryPrice.toFixed(2)}` : ''; const exits = [r.stop != null ? `STOP ${r.stop.toFixed(2)}` : null, r.trail ? `TRAIL ${r.trail.offset}${r.trail.type === 'PERCENT' ? '%' : r.trail.type === 'VALUE' ? '$' : ' ticks'} ${r.trail.basis ?? 'LAST'}` : null, ...r.targets.map((t: any, i: number) => `TARGET${i + 1} ${Number(t.price).toFixed(2)}${t.qty_pct && t.qty_pct !== 100 ? ` ${t.qty_pct}%` : ''}`)].filter(Boolean).join(' | '); return `${r.side} ${r.qty} ${r.symbol} ${r.entryType}${price} ${r.tif}${exits ? ` | ${exits}` : ''}` }
function csv(v: any) { const s = str(v); return /[",\n]/.test(s) ? `"${s.replace(/"/g, '""')}"` : s }
function csvText(rows: SetupRow[]) { const out: string[][] = [['Source','Symbol','Company','Rating','RatingSource','AnalystRaw','AnalystCount','Upside','Account','Side','Qty','EntryType','EntryPrice','Stop','Targets','TIF','Decision','Score','Sector','Reason','SetupLine']]; rows.forEach(r => { const rr = deriveTradeAiRating(r); out.push([r.source, r.symbol, str(r.company), ratingLabel(rr.rating), rr.source, str(r.analyst), str(r.analystCount), str(r.analystUpside), r.account, r.side, String(r.qty), r.entryType, str(r.entryPrice), str(r.stop), r.targets.map((t: any) => t.price).join('|'), r.tif, str(r.decision), str(r.score), str(r.sector), str(r.reason), setupLine(r)]) }); return out.map(r => r.map(csv).join(',')).join('\n') + '\n' }
function copyText(text: string, setMsg: (s: string) => void) { const done = () => { setMsg('copied'); setTimeout(() => setMsg(''), 1200) }; if (navigator.clipboard?.writeText) navigator.clipboard.writeText(text).then(done).catch(done); else { const ta = document.createElement('textarea'); ta.value = text; document.body.appendChild(ta); ta.select(); document.execCommand('copy'); document.body.removeChild(ta); done() } }
function dl(name: string, type: string, text: string) { const b = new Blob([text], { type }); const u = URL.createObjectURL(b); const a = document.createElement('a'); a.href = u; a.download = name; document.body.appendChild(a); a.click(); a.remove(); URL.revokeObjectURL(u) }
function activityHit(r: SetupRow, activity: any[]) { const exactAcct = r.account && r.account !== 'ANY'; return activity.find(a => { const sym = String(a.symbol ?? '').toUpperCase() === r.symbol; const ac = !exactAcct || String(a.account_key ?? '') === r.account; const aq = first(a.quantity, a.qty, a.filled_qty, a.shares); const q = aq == null || Number(aq) === r.qty || !r.qty; const sideRaw = String(first(a.side, a.action, a.instruction, '')).toUpperCase(); const sideOk = !sideRaw || r.side.includes(sideRaw) || sideRaw.includes(r.side.split(' ')[0]); return sym && ac && q && sideOk }) ?? null }
function rowState(r: SetupRow, local: LocalState, activity: any[]): SetupState { if (!r.entryPrice) return 'EXCEPTION'; if (activityHit(r, activity)) return 'BROKER_OBSERVED'; return local[r.id]?.generated ? 'GENERATED' : 'READY' }
function payload(r: SetupRow, state: SetupState, hit: any, account: AccountSnapshot | null) { const derived = deriveTradeAiRating(r); const sz = sizing(r, account); return { mode: 'MANUAL_TOS', source: r.source, source_id: r.id, account: r.account, account_snapshot: account, sizing: sz, symbol: r.symbol, company: r.company, rating: derived.rating, rating_source: derived.source, analyst_raw: r.analyst, analyst_count: r.analystCount, analyst_upside: r.analystUpside, side: r.side, qty: r.qty, entry_type: r.entryType, entry_price: r.entryPrice, stop: r.stop, targets: r.targets, tif: r.tif, score: r.score, advisory: decisionText(r), reason: r.reason, broker_observed: !!hit, observed: hit ? { account_key: hit.account_key, kind: hit.kind, status: hit.status, captured_at: hit.captured_at } : null, setup_line: setupLine(r) } }
function badgeColor(source: SourceKind) { return source === 'PROPOSAL' ? C.green : source === 'WATCHLIST' ? C.blue : C.purple }
function statusColor(state: SetupState) { return state === 'BROKER_OBSERVED' ? C.green : state === 'GENERATED' ? C.blue : state === 'EXCEPTION' ? C.red : C.dim }

export default function ManualTosDesk() {
  const { data: draftsR, refetch } = useApi<any>('/api/v2/broker-orders/drafts?broker=schwab', 30_000)
  const { data: activityR } = useApi<any>('/api/v2/broker-orders/activity', 30_000)
  const { data: reconR } = useApi<any>('/api/v2/broker-orders/shadow-recon', 60_000)
  const { data: proposalsR } = useApi<any>('/api/v2/paper-proposals', 60_000)
  const { data: scannerR } = useApi<any>('/api/v2/trade-ai', 60_000)
  const { data: watchlistR } = useApi<any>('/api/v2/watchlist/items?sort=hermes', 60_000)
  const { data: scards } = useApi<any>('/api/v2/symbol-cards', 300_000)
  const { data: schwabR } = useApi<any>('/api/v2/schwab/accounts-live', 35_000)
  const paMap = useProAnalystMap()
  const cardMap: Record<string, any> = (scards as any)?.cards ?? {}
  const accountRows: any[] = ((schwabR as any)?.data ?? schwabR)?.accounts ?? []
  const accountMap: Record<string, AccountSnapshot> = Object.fromEntries(accountRows.map(a => { const s = accountSnapshot(a); return [s.key, s] }).filter(([k]) => !!k))
  const [local, setLocal] = useState<LocalState>({})
  const [prefs, setPrefs] = useState<UiPrefs>(defaultPrefs)
  const [msg, setMsg] = useState('')
  useEffect(() => { try { setLocal(JSON.parse(localStorage.getItem(LS_KEY) || '{}')) } catch { setLocal({}) }; try { setPrefs({ ...defaultPrefs, ...JSON.parse(localStorage.getItem(PREF_KEY) || '{}') }) } catch { setPrefs(defaultPrefs) } }, [])
  const saveLocal = (n: LocalState) => { setLocal(n); localStorage.setItem(LS_KEY, JSON.stringify(n)) }
  const savePrefs = (patch: Partial<UiPrefs>) => { const next = { ...prefs, ...patch }; setPrefs(next); localStorage.setItem(PREF_KEY, JSON.stringify(next)) }
  const drill = (patch: Partial<UiPrefs>) => savePrefs({ ...defaultPrefs, ...patch })

  const rows = useMemo(() => { const p = (((proposalsR as any)?.proposals ?? []) as any[]).map(x => fromProposal(x, local, paMap)).filter(Boolean) as SetupRow[]; const wl = (((watchlistR as any)?.items ?? []) as any[]).map(x => fromWatchlistItem(x, local, paMap, cardMap)).filter(Boolean) as SetupRow[]; const scanner = wl.length ? [] : (((scannerR as any)?.tickers ?? []) as any[]).filter(x => ['GO','WAIT'].includes(String(x.decision ?? '').toUpperCase()) || Number(x.score ?? 0) >= 30).map(x => fromTradeAiScanner(x, local, paMap)).filter(Boolean) as SetupRow[]; const d = (((draftsR as any)?.drafts ?? []) as any[]).map(x => fromDraft(x, local, paMap)).filter(Boolean) as SetupRow[]; const seen: Record<string, SetupRow> = {}; [...p, ...wl, ...scanner, ...d].forEach(r => { const k = `${r.source}:${r.id}`; if (!seen[k]) seen[k] = r }); return Object.values(seen) }, [proposalsR, watchlistR, scannerR, draftsR, local, paMap, cardMap])
  const activity = ((activityR as any)?.activity ?? []) as any[]
  const wrapped = rows.map(r => ({ r, state: rowState(r, local, activity), hit: activityHit(r, activity) }))
  const filtered = wrapped.filter(({ r, state }) => { const decision = normalizedDecision(r.decision); const rating = deriveTradeAiRating(r).rating; const q = prefs.search.trim().toUpperCase(); if (prefs.source !== 'ALL' && r.source !== prefs.source) return false; if (prefs.account !== 'ALL' && r.account !== prefs.account) return false; if (prefs.decision !== 'ALL' && decision !== prefs.decision) return false; if (prefs.analyst !== 'ALL' && rating !== prefs.analyst) return false; if (prefs.status !== 'ALL' && state !== prefs.status) return false; if (q && !`${r.symbol} ${r.company ?? ''} ${r.reason ?? ''} ${r.analyst ?? ''} ${ratingLabel(rating)}`.toUpperCase().includes(q)) return false; if (prefs.minScore && scoreNum(r.score) < Number(prefs.minScore)) return false; if (prefs.hasEntry && !r.entryPrice) return false; if (prefs.hasRisk && !r.stop && r.targets.length === 0) return false; return true })
  const visible = [...filtered].sort((a, b) => { if (prefs.sort === 'SYMBOL_ASC') return a.r.symbol.localeCompare(b.r.symbol); if (prefs.sort === 'SOURCE') return a.r.source.localeCompare(b.r.source) || a.r.symbol.localeCompare(b.r.symbol); if (prefs.sort === 'STATUS') return a.state.localeCompare(b.state) || a.r.symbol.localeCompare(b.r.symbol); if (prefs.sort === 'ENTRY_PRICE') return (b.r.entryPrice ?? 0) - (a.r.entryPrice ?? 0); if (prefs.sort === 'ANALYST') return ratingRank(deriveTradeAiRating(b.r).rating) - ratingRank(deriveTradeAiRating(a.r).rating) || scoreNum(b.r.score) - scoreNum(a.r.score); return scoreNum(b.r.score) - scoreNum(a.r.score) })
  const counts = { total: rows.length, proposals: rows.filter(r => r.source === 'PROPOSAL').length, watchlist: rows.filter(r => r.source === 'WATCHLIST').length, drafts: rows.filter(r => r.source === 'DRAFT').length, ready: wrapped.filter(x => x.state === 'READY').length, generated: wrapped.filter(x => x.state === 'GENERATED').length, observed: wrapped.filter(x => x.state === 'BROKER_OBSERVED').length, exceptions: wrapped.filter(x => x.state === 'EXCEPTION').length }
  const ratingCounts: Record<ManualTosRating, number> = { STRONG_BUY: 0, BUY: 0, HOLD: 0, SELL: 0, STRONG_SELL: 0, UNKNOWN: 0 }; rows.forEach(r => { ratingCounts[deriveTradeAiRating(r).rating] += 1 })
  const lastRun = (reconR as any)?.runs?.[0]
  const watchTxt = visible.map(x => x.r.symbol).filter((s, i, a) => a.indexOf(s) === i).join('\n') + '\n'
  const setField = (id: string, patch: Partial<LocalState[string]>) => saveLocal({ ...local, [id]: { ...(local[id] ?? {}), ...patch } })
  const kpis = [ { label: 'Total Candidates', value: counts.total, color: 'var(--text0)', onClick: () => drill({}) }, { label: 'Proposals', value: counts.proposals, color: C.green, onClick: () => drill({ source: 'PROPOSAL' }) }, { label: 'Watchlist', value: counts.watchlist, color: C.blue, onClick: () => drill({ source: 'WATCHLIST' }) }, { label: 'Drafts', value: counts.drafts, color: C.purple, onClick: () => drill({ source: 'DRAFT' }) }, { label: 'Ready', value: counts.ready, color: C.dim, onClick: () => drill({ status: 'READY' }) }, { label: 'Generated', value: counts.generated, color: C.blue, onClick: () => drill({ status: 'GENERATED' }) }, { label: 'Broker Observed', value: counts.observed, color: C.green, onClick: () => drill({ status: 'BROKER_OBSERVED' }) }, { label: 'Exceptions', value: counts.exceptions, color: C.red, onClick: () => drill({ status: 'EXCEPTION' }) } ]

  return <div>
    <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'baseline', marginBottom: 14 }}><div><div style={{ fontSize: 22, fontWeight: 900 }}>Manual ToS Execution Desk</div><div style={{ fontSize: 11, color: C.dim }}>Aligned with curated Watchlist. Select an account to see read-only cash/buying-power sizing, notional exposure and R:R.</div></div><button onClick={() => refetch()} style={btn('var(--bg2)', C.blue)}>refresh</button></div>
    <div style={{ display: 'grid', gridTemplateColumns: 'repeat(8,minmax(100px,1fr))', gap: 8, marginBottom: 12 }}>{kpis.map(k => <button key={k.label} onClick={k.onClick} title={`Filter to ${k.label}`} style={{ textAlign: 'left', background: 'var(--bg1)', border: '1px solid var(--border)', borderRadius: 9, padding: '8px 10px', cursor: 'pointer' }}><div style={{ fontSize: 16, fontWeight: 900, color: k.color }}>{k.value}</div><div style={{ fontSize: 8, color: C.dim, textTransform: 'uppercase' }}>{k.label}</div></button>)}</div>
    <div style={{ display: 'grid', gridTemplateColumns: '1.2fr 1fr', gap: 12, marginBottom: 12 }}><div style={{ background: 'var(--bg1)', border: '1px solid var(--border)', borderRadius: 10, padding: 12 }}><div style={{ fontSize: 12, fontWeight: 800, marginBottom: 8 }}>Enterprise workflow</div><div style={{ display: 'grid', gridTemplateColumns: 'repeat(5,1fr)', gap: 8 }}>{['Candidate','Ticket Generated','Operator Enters in Thinkorswim','Schwab Read-Only Observes','Trade AI Tracks / Reconciles'].map((s, i) => <div key={s} style={{ background: 'var(--bg2)', borderRadius: 8, padding: 8, minHeight: 48 }}><div style={{ fontSize: 9, color: C.blue, fontWeight: 900 }}>STEP {i + 1}</div><div style={{ fontSize: 10, color: 'var(--text0)', marginTop: 3 }}>{s}</div></div>)}</div></div><div style={{ background: 'var(--bg1)', border: '1px solid var(--border)', borderRadius: 10, padding: 12 }}><div style={{ fontSize: 12, fontWeight: 800 }}>Read-only reconciliation</div><div style={{ fontSize: 10, color: C.dim, marginTop: 6 }}>{lastRun ? `Last run #${lastRun.id} · orders ${lastRun.orders_seen} · matched ${lastRun.matched} · mismatched ${lastRun.mismatched} · ${lastRun.status}` : 'No recon run yet'}</div><div style={{ fontSize: 9, color: C.amber, marginTop: 8 }}>No local button creates truth. Account cash/buying power is read-only; no Schwab write path is present.</div></div></div>
    <div style={{ padding: 12, border: '1px solid var(--border)', borderRadius: 10, background: 'var(--bg1)', marginBottom: 12 }}>
      <div style={{ display: 'flex', gap: 8, flexWrap: 'wrap', alignItems: 'center', marginBottom: 10 }}><span style={{ fontSize: 10, color: C.dim, fontWeight: 800 }}>Recommendation filter:</span><button onClick={() => savePrefs({ analyst: 'ALL' })} style={{ ...btn(prefs.analyst === 'ALL' ? C.blue : 'var(--bg2)', prefs.analyst === 'ALL' ? '#0b1020' : 'var(--text2)'), border: `1px solid ${prefs.analyst === 'ALL' ? C.blue : 'var(--border)'}` }}>All · {rows.length}</button>{(['STRONG_BUY','BUY','HOLD','SELL','STRONG_SELL','UNKNOWN'] as ManualTosRating[]).map(a => <button key={a} onClick={() => savePrefs({ analyst: a })} style={{ ...btn(prefs.analyst === a ? analystColor(a) : 'var(--bg2)', prefs.analyst === a ? '#0b1020' : 'var(--text2)'), border: `1px solid ${prefs.analyst === a ? analystColor(a) : 'var(--border)'}` }}>{ratingLabel(a)} · {ratingCounts[a]}</button>)}</div>
      <div style={{ display: 'grid', gridTemplateColumns: 'repeat(4, minmax(120px,1fr))', gap: 8, marginBottom: 8 }}><select value={prefs.source} onChange={e => savePrefs({ source: e.target.value as SourceFilter })} style={inputStyle}><option value="ALL">All sources</option><option value="PROPOSAL">Proposal</option><option value="WATCHLIST">Watchlist</option><option value="DRAFT">Draft</option></select><select value={prefs.account} onChange={e => savePrefs({ account: e.target.value as AccountFilter })} style={inputStyle}><option value="ALL">All account selections</option><option value="ANY">Any Schwab</option><option value="schwab_taxable">Taxable</option><option value="schwab_rollover_ira">Rollover IRA</option><option value="schwab_roth_ira">Roth IRA</option></select><select value={prefs.decision} onChange={e => savePrefs({ decision: e.target.value as DecisionFilter })} style={inputStyle}><option value="ALL">All decisions</option><option value="GO">GO</option><option value="WAIT">WAIT</option><option value="OTHER">Other</option></select><select value={prefs.status} onChange={e => savePrefs({ status: e.target.value as StatusFilter })} style={inputStyle}><option value="ALL">All statuses</option><option value="READY">Ready</option><option value="GENERATED">Generated</option><option value="BROKER_OBSERVED">Broker observed</option><option value="EXCEPTION">Exception</option></select><input value={prefs.search} onChange={e => savePrefs({ search: e.target.value })} placeholder="Search symbol/company/reason/rating" style={inputStyle} /><input value={prefs.minScore} onChange={e => savePrefs({ minScore: e.target.value })} placeholder="Min score" type="number" style={inputStyle} /><select value={prefs.sort} onChange={e => savePrefs({ sort: e.target.value as SortMode })} style={inputStyle}><option value="ANALYST">Recommendation strength</option><option value="SCORE_DESC">Score desc</option><option value="SYMBOL_ASC">Symbol A-Z</option><option value="SOURCE">Source</option><option value="STATUS">Status</option><option value="ENTRY_PRICE">Entry price</option></select><div style={{ display: 'flex', gap: 10, alignItems: 'center', fontSize: 10, color: C.dim }}><label><input type="checkbox" checked={prefs.hasEntry} onChange={e => savePrefs({ hasEntry: e.target.checked })} /> Has entry</label><label><input type="checkbox" checked={prefs.hasRisk} onChange={e => savePrefs({ hasRisk: e.target.checked })} /> Has stop/target</label></div></div>
      <div style={{ display: 'flex', gap: 8, flexWrap: 'wrap', alignItems: 'center' }}><span style={{ fontSize: 11, color: C.dim }}>Visible exports:</span><button style={btn('#475569')} onClick={() => savePrefs(defaultPrefs)}>Reset Filters</button><button style={btn('#1d4ed8')} onClick={() => copyText(watchTxt, setMsg)}>copy visible symbols</button><button style={btn('#334155')} onClick={() => dl('tradeai_tos_watchlist.txt', 'text/plain', watchTxt)}>download Thinkorswim watchlist .txt</button><button style={btn('#334155')} onClick={() => dl('tradeai_manual_setups.csv', 'text/csv', csvText(visible.map(x => x.r)))}>download setup CSV</button>{msg && <span style={{ fontSize: 10, color: C.green }}>{msg}</span>}</div>
    </div>
    {visible.length === 0 && <div style={{ padding: 14, border: '1px solid var(--border)', borderRadius: 10, background: 'var(--bg1)', color: C.dim, fontSize: 11 }}>No candidates match the current filters. Use Total Candidates or Reset Filters to clear saved browser filters.</div>}
    {visible.map(({ r, state, hit }) => { const rating = deriveTradeAiRating(r); const account = r.account === 'ANY' ? null : accountMap[r.account] ?? null; const sz = sizing(r, account); return <div key={`${r.source}-${r.id}`} style={{ border: `1px solid ${state === 'BROKER_OBSERVED' ? C.green : state === 'EXCEPTION' ? C.red : 'var(--border)'}`, borderRadius: 10, background: 'var(--bg1)', padding: 13, marginBottom: 10 }}><div style={{ display: 'grid', gridTemplateColumns: '1.22fr 1.05fr auto', gap: 12, alignItems: 'start' }}><div><div style={{ display: 'flex', gap: 8, alignItems: 'center', flexWrap: 'wrap' }}><span style={{ fontSize: 9, color: badgeColor(r.source), fontWeight: 900 }}>{r.source}</span><span style={{ fontSize: 17, fontWeight: 900, ...mono }}>{r.symbol}</span>{r.company && <span style={{ fontSize: 10, color: C.dim }}>{r.company}</span>}<span style={{ fontSize: 9, color: statusColor(state), background: `${statusColor(state)}22`, padding: '2px 7px', borderRadius: 4 }}>{state === 'BROKER_OBSERVED' ? 'AUTO-LINKED FROM SCHWAB READ-ONLY' : state}</span></div><div style={{ display: 'flex', gap: 7, flexWrap: 'wrap', marginTop: 6, fontSize: 9 }}><span style={{ color: C.amber }}>{decisionText(r)}</span><span style={{ color: analystColor(r) }}>{rating.source === 'analyst' ? 'analyst' : 'Trade AI rating'} {ratingLabel(rating.rating)}</span>{r.analystCount != null && <span style={{ color: C.dim }}>{r.analystCount} analysts</span>}{r.analystUpside != null && <span style={{ color: Number(r.analystUpside) >= 0 ? C.green : C.red }}>{Number(r.analystUpside) >= 0 ? '+' : ''}{r.analystUpside}%</span>}<span style={{ color: C.blue }}>score {str(r.score) || 'n/a'}</span>{r.sector && <span style={{ color: C.dim }}>{r.sector}</span>}{r.reason && <span style={{ color: C.dim }}>{String(r.reason).slice(0, 180)}</span>}</div><div style={{ marginTop: 8, fontSize: 12, fontWeight: 800, ...mono }}>{setupLine(r)}</div><div style={{ display: 'flex', gap: 8, flexWrap: 'wrap', marginTop: 7, fontSize: 9 }}>{r.entryPrice != null && <span>entry {r.entryType} {r.entryPrice.toFixed(2)}</span>}{r.stop != null && <span style={{ color: C.red }}>stop {r.stop.toFixed(2)}</span>}{r.targets.map((t: any, i: number) => <span key={i} style={{ color: C.green }}>target {i + 1}: {Number(t.price).toFixed(2)}</span>)}{r.trail && <span style={{ color: C.purple }}>trail {r.trail.offset}</span>}</div>{hit ? <div style={{ marginTop: 7, fontSize: 10, color: C.green }}>Observed account {acct(hit.account_key)} · {hit.kind ?? 'activity'} · {hit.status ?? 'status n/a'} · {String(hit.captured_at ?? '').slice(0, 16)}</div> : <div style={{ marginTop: 7, fontSize: 10, color: C.dim }}>No matching Schwab activity observed yet.</div>}</div><div><div style={{ display: 'grid', gridTemplateColumns: '1fr 70px', gap: 8, marginBottom: 7 }}><select value={r.account} onChange={e => setField(r.id, { account: e.target.value })} style={inputStyle}>{['ANY', ...Object.keys(accountMap).filter(k => k && !ACCOUNTS.includes(k)), ...ACCOUNTS.filter(a => a !== 'ANY')].filter((v, i, arr) => arr.indexOf(v) === i).map(a => <option key={a} value={a}>{acct(a)}</option>)}</select><input value={r.qty} onChange={e => setField(r.id, { qty: Number(e.target.value) || 1 })} style={inputStyle} /></div><div style={{ display: 'grid', gridTemplateColumns: 'repeat(3,1fr)', gap: 6 }}><div style={metricBox}><div style={{ fontSize: 8, color: C.dim }}>Cash/BP</div><div style={{ fontSize: 11, fontWeight: 800 }}>{account ? money(account.cash ?? account.buyingPower) : 'select account'}</div></div><div style={metricBox}><div style={{ fontSize: 8, color: C.dim }}>Notional</div><div style={{ fontSize: 11, fontWeight: 800 }}>{money(sz.notional)}</div></div><div style={metricBox}><div style={{ fontSize: 8, color: C.dim }}>% Cash</div><div style={{ fontSize: 11, fontWeight: 800, color: sz.pctCash && sz.pctCash > 10 ? C.amber : 'var(--text0)' }}>{pct(sz.pctCash)}</div></div><div style={metricBox}><div style={{ fontSize: 8, color: C.dim }}>Acct Value</div><div style={{ fontSize: 11, fontWeight: 800 }}>{account ? money(account.accountValue) : '—'}</div></div><div style={metricBox}><div style={{ fontSize: 8, color: C.dim }}>% Account</div><div style={{ fontSize: 11, fontWeight: 800 }}>{pct(sz.pctAccount)}</div></div><div style={metricBox}><div style={{ fontSize: 8, color: C.dim }}>Risk / R:R</div><div style={{ fontSize: 11, fontWeight: 800, color: sz.rr && sz.rr >= 2 ? C.green : C.amber }}>{money(sz.risk)} / {sz.rr ? sz.rr.toFixed(2) : '—'}</div></div></div><div style={{ marginTop: 6, fontSize: 9, color: C.dim }}>Sizing is read-only math from selected account + entry/stop/target. It does not place or queue an order.</div></div><div style={{ display: 'flex', gap: 6, flexWrap: 'wrap', justifyContent: 'flex-end' }}><button onClick={() => { setField(r.id, { generated: true }); copyText(setupLine(r), setMsg) }} style={btn('#1d4ed8')}>Copy setup</button><button onClick={() => dl(`tradeai_${r.symbol}.json`, 'application/json', JSON.stringify(payload(r, state, hit, account), null, 2) + '\n')} style={btn('#334155')}>Export JSON</button><button onClick={() => dl(`tradeai_${r.symbol}.html`, 'text/html', `<pre>${setupLine(r)}\n\n${JSON.stringify(payload(r, state, hit, account), null, 2)}</pre>`)} style={btn('#334155')}>Export HTML</button></div></div></div> })}
    <div style={{ marginTop: 12, fontSize: 8.5, color: C.dim }}>Sources: /api/v2/watchlist/items?sort=hermes, /api/v2/pro-analyst/pills, /api/v2/schwab/accounts-live, /api/v2/paper-proposals, /api/v2/broker-orders/drafts, /api/v2/broker-orders/activity. Advisory/manual setup only; no broker write route is present.</div>
  </div>
}
