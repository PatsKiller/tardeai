# Trade AI Reports v12 — Python dependencies
# Install: pip install -r requirements.txt

# Core data
pandas>=2.2.0
pyyaml>=6.0
python-dotenv>=1.0
requests>=2.31

# Market calendar (NYSE holiday detection)
exchange-calendars>=4.5

# PDF generation
reportlab>=4.1

# Word document generation
python-docx>=1.1

# LLM — Anthropic (primary)
anthropic>=0.25

# LLM — OpenAI (optional fallback)
openai>=1.30

# Catalyst APIs
finnhub-python>=2.4
# polygon, newsapi, fmp, alpha_vantage accessed via requests (no extra package needed)

# WhatsApp / alerts
twilio>=8.12
