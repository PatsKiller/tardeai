"""Broker abstraction layer (ADR-B1) — canonical order intents, capability registry, fail-closed execution
guard, pure translators. Schwab is BROKER_DISABLED this phase; Alpaca paper training is untouched."""
